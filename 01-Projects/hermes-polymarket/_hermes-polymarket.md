---
type: project
statut: actif
tags: [trading, agents, automation]
cree: 2026-07-03
---

# Hermes Polymarket

## Objectif
Système de trading autonome sur Polymarket, 24/7 sur le VPS Hetzner : la décision de trade vit dans des **moteurs Python déterministes**, Hermes Agent n'est que le cockpit (monitoring/ops, lecture seule). Roadmap 3 bots — arbitrage, copy-trading (Atlas), BTC-5m (gaté) — en paper-first avec HITL Telegram ; passage en réel seulement après gates de validation.

## Statut
**Au 2026-07-30** *(ssh lecture seule, verdict Atlas relu — aucune écriture VPS ; log session 30/07)*
- **Verdict Atlas (Bot 2)** — le one-shot de fin de fenêtre A/B paper (~14 j) a bien tiré le **2026-07-17 18:30 UTC** (`atlas-validation-review`, exit 0, poussé Telegram). Chiffres « à arbitrer », le cron ne juge pas :
  - wallet `0xd1acd392…08d5` : 4 clôturées, winrate 25%, **total +$8.66** — échantillon trop mince pour conclure ;
  - wallet `0x44886115…e319` : 53 clôturées, winrate 43%, **total −$23.53** — le miroir actif perd (pertes > gains). Combiné ≈ **−$15** sur bankroll notionnel $50.
  - Arbitrage keep/adjust/stop = décision d'Hector en conversation. **Observé au 30/07 : Atlas toujours en paper** (timers `atlas`/`copy-tracker`/`atlas-report` actifs, DB fraîches) → keep-running de facto, aucune clé réelle.
- **Bot 1 (`polymarket_trader`)** — **pleinement opérationnel en paper** : chaîne snapshot→brain→dispatcher→executor→monitor firing à l'heure (dernier cycle ~11:3x UTC le 30/07), aucun timer Bot 1 en échec.
- **Gate Bot 3 (BTC-5m)** — **a bougé** : de « gaté, plan seul (06/07) » à un **collecteur record-only** en construction dans `~/bot3_btc5min` (repo git dédié, 2 commits : squelette record-only épinglé → dead-man's switch T1/T2 passés). Heartbeat minute **VERT** vers healthchecks.io (db 8 tables / 16 triggers, 31,5 G libres). **Toujours zéro trade** : la gate d'exécution reste fermée, seule la fondation collecte + surveillance tourne.
- *Nota : 3 unités `run-uXXXX` en échec dans `--failed` = transients d'un test de sandbox ads-studio, sans rapport avec Hermes.*

---

Au 2026-07-04 : **3 GO en attente** — config A/B Flipadelphia + Kickstand7 (25 $ chacun, remplace Sharkbets), fix de la persistance recency du finder, sanity-check des timestamps de verdicts API. Formule d'edge du Bot 1 corrigée (Σbid − 1). Bot 3 toujours gaté.

## Next actions
- [ ] 🔴 **LE VRAI LEVIER N'EST PAS TRIGGER.DEV, C'EST `bot3_trim` — et c'est le seul geste qui change la TRAJECTOIRE.** Arrêter Trigger.dev rend **21 Gio d'un coup** mais ne retire que **0,15 Go/j** de la pente : **ça achète environ huit jours.** `bot3_trim` rend **~6 Gio** *et* retire **~0,46 Gio/j** — c'est-à-dire **plus de la moitié de la pente restante**. Un gain ponctuel contre un changement de pente : ce n'est pas la même nature de geste. *(Formulé par Hector le 2026-08-23 : « Trigger.dev n'achète que 8 jours, bot3_trim change la trajectoire ».)*
  - **Il devient exécutable dès l'étape 5 de l'arrêt** : sa garde exige `taille + 5 Gio` = **21,70 Gio** libres ; on en aura **~26,3**. Marge de 4,6 Gio — ça passe, mais sans confort.
  - **Deux préalables, dans cet ordre.** (1) **Corriger le `--dry-run` qui contourne les deux gardes** et écrit quand même la base neuve — voir la next action dédiée ; tant que ce n'est pas fait, le mode « sans risque » reste le plus dangereux. (2) Savoir qu'il **conserve l'ancienne base 7 jours** (`GARDE_PRE_TRIM_J = 7`) : **l'espace ne revient qu'au bout d'une semaine**, et pendant cette semaine le disque porte les deux bases.
  - **Et le geste qui manque vraiment : l'armer en timer hebdomadaire.** Il n'en a **aucun** aujourd'hui — c'est pour cela qu'il n'a jamais tourné depuis son écriture le 03/08. Une coupe unique repousse l'échéance ; une coupe hebdomadaire **borne `trades` pour de bon** et ramène la croissance de `bot3.db` de 0,849 à ~0,39 Gio/j.
- [x] ~~🔴 **PLAN D'AGRANDISSEMENT HETZNER — volume de 300 Go, ~6 mois achetés, rien créé au 2026-08-23.**~~ — **ÉCARTÉ le 2026-08-23 : Hector arbitre pour l'arrêt de Trigger.dev plutôt que pour l'agrandissement.** Le plan reste écrit et valide si la question se rouvre — [[01-Projects/hermes-polymarket/plan-agrandissement-volume-hetzner|plan-agrandissement-volume-hetzner]]. Chiffrage conservé ci-dessous parce qu'il reste la seule voie qui achète des *années* plutôt que des mois. **Analyse d'origine :** La machine est bien un **Hetzner Cloud vServer** (instance `126039211`, `nbg1-dc3`), donc éligible aux Cloud Volumes ; aucun volume n'est attaché, `sda` fait 76,3 Go d'un seul tenant. **Prix relevé : ~0,04 €/Go/mois HT** — *chiffre issu de sources tierces, non rendu par les pages tarifaires de Hetzner (JS) : à confirmer en console avant création.* Soit **~12 €/mois HT, ~14,40 € TTC** pour 300 Go.
  - **Le dimensionnement ne se divise pas naïvement : la capacité exigée croît 1,6× plus vite que la base.** Parce que la garde du backup réserve un pic proportionnel au fichier — `requis_pic = 0,48 × db × (1 + 1/13,60) = 0,515 × db` — la capacité utile nécessaire vaut `1,586 × db + 5 Gio`. Avec `db(t) = 16,70 + 0,849·t` (pente que `report_daily` mesure lui-même sur 3 jours), le volume se consomme à **1,35 Gio/jour**, pas 0,85.

    | volume | utile (ext4 `-m 0`) | tenue **sans** rétention | tenue **avec** `bot3_trim` hebdo | prix HT |
    |---|---|---|---|---|
    | 100 Go | 91,6 Gio | 45 j — **1,5 mois** | ~4,7 mois | 4 € |
    | 200 Go | 183,3 Gio | 113 j — **3,7 mois** | ~8,6 mois | 8 € |
    | **300 Go** | **274,9 Gio** | **181 j — 5,9 mois** | **~13,4 mois** | **12 €** |
    | 500 Go | 458,2 Gio | 317 j — **10,4 mois** | ~22 mois | 20 € |

    **300 Go recommandé** : six mois couvrent la rentrée Ynov sans qu'aucune décision de rétention soit forcée dans l'urgence, et treize si `bot3_trim` se met à tourner. **Un volume Hetzner s'agrandit à chaud mais ne se réduit JAMAIS** — 300 Go est la taille où l'on cesse de rouvrir le sujet, sans payer pour du vide.
  - **Le geste : fenêtre d'arrêt obligatoire, ~5 à 8 minutes. Pas à chaud.** Deux processus tiennent `bot3.db` ouverte, le WAL est vivant (31 Mio, écrit à la minute) : une copie à chaud donne une base déchirée, et un `.backup` SQLite cohérent serait périmé dès la seconde suivante puisque le collecteur continuerait d'écrire sur l'ancien inode. Ordre : arrêter `bot3-collector.service` + les timers `bot3-backfill` / `bot3-heartbeat` / `bot3-report` (les trois premiers sont exactement le tuple `UNITES` de `bot3_trim`), vérifier par `fuser` que plus rien ne tient la base, `PRAGMA wal_checkpoint(TRUNCATE)`, relever **avant** (sha256, `PRAGMA integrity_check`, `user_version` = 3, **16 triggers**, compte de lignes des 8 tables), copier `data/` entière sur le volume, **revérifier les mêmes empreintes après** — et **prouver R5 par le test, pas par la lecture** : tenter un `DELETE` sur la copie et vérifier qu'il `ABORT`, comme le fait `bot3_trim` à sa ligne 186.
  - **Symlink, PAS montage direct sur `~/bot3_btc5min/data` — et c'est la décision qui compte.** `DB_PATH = ROOT / "data" / "bot3.db"` est **codé en dur** dans `db.py:16` (aucune variable d'environnement ne le porte). Si l'on montait le volume *par-dessus* `data/`, un montage manqué au redémarrage exposerait le répertoire d'origine resté dessous : le collecteur écrirait alors joyeusement dans une base périmée, **sans rien signaler**. Exactement la famille de pannes qu'on traque depuis ce matin. Avec `/mnt/hc-bot3` + un symlink `data → /mnt/hc-bot3/data`, un volume absent fait **crasher le service au démarrage**. `nofail` en fstab pour que ce soit le service qui hurle, pas le boot.
  - **Ne pas effacer l'ancienne `data/` avant que le premier backup ait réussi depuis le volume.** Critère écrit d'avance : une archive datée du jour dans le nouveau `backups/`, `verifier_archive` passée. Tant que l'ancienne copie reste, `/` ne récupère pas ses 17,6 Gio — mais le collecteur n'y écrit déjà plus, et c'est ça qui compte d'abord.
  - **La séquence exacte, commande par commande, est écrite : [[01-Projects/hermes-polymarket/plan-agrandissement-volume-hetzner|plan-agrandissement-volume-hetzner]]** — phase 0 entièrement réversible hors fenêtre, fenêtre d'arrêt balisée, **deux points de non-retour marqués** (le redémarrage du collecteur, puis la suppression de l'ancienne `data/`), et un contrôle bloquant à chaque étape.
  - **⚠️ Le raccourci « ne déplacer que `backups/` » est un piège.** Il marche sur le papier — `cout_db = clair_estime if meme_fs else 0`, donc mettre le backup sur un autre FS fait tomber l'exigence côté base de 8,5 Gio à zéro, et `marge_db = 5,75 Gio ≥ 5,0` passerait. **Mais de 0,75 Gio seulement, soit ~15 heures** au rythme de `/`. Le filet reviendrait pour une nuit.
- [ ] 🔴 **NE PAS LANCER `bot3_trim.py --dry-run` — il remplirait le disque. Le piège le plus dangereux du serveur porte le nom le plus rassurant.** `--dry-run` **contourne les DEUX gardes** : celle du seuil (`if not args.go and not args.dry_run and dispo >= SEUIL_GO`) **et** celle de l'espace (`if dispo < besoin and not args.dry_run`). Il appelle ensuite `construire()` comme un run normal et **laisse la base neuve en place** — « la base neuve est laissée pour inspection ». `trades` pèse ~48 % de `bot3.db`, donc **~8,6 Go écrits pour 5,5 Go libres.** Le run réel, lui, refuse proprement (`besoin = taille + 5 Gio` = 23,3 Go > 5,5 → exit 3) : **c'est le mode « sans risque » qui est armé, et le mode réel qui protège.** Même famille que le défaut du 03/08 — *un garde-fou qui n'en est pas un* — sauf que celui-ci est inversé. Correctif possible quand ce sera le moment : faire porter la garde d'espace **aussi** sur `--dry-run`, puisque c'est l'écriture de la base neuve qui coûte, pas l'échange. *(Trouvé le 2026-08-23. Le défaut du 03/08, lui, est bien corrigé : le script distingue désormais la production.)*
- [ ] **2026-08-29 — REVUE DU DISQUE, deux jours avant que le mur du backup ne revienne. Ce que l'arrêt de Trigger.dev NE règle PAS.** *(Date recalée le 23/08 au soir : elle était au 14/09 dans le scénario du volume, qui a été écarté. Avec l'arrêt de la pile à la place, l'échéance est **trois semaines plus tôt** — une revue posée après l'alarme n'est pas une revue.)*
  - **Un gain ponctuel n'est pas un changement de pente.** L'arrêt complet rend 21 Gio mais ne retire que 0,15 Go/j : `/` passerait à ~26,3 Gio à **1,07 Go/j**. Mur du backup **~2026-09-01**, alarme heartbeat ~03/09, disque plein ~18/09. **Huit jours.**
  - **Ce qui reste à faire, dans l'ordre où chacun débloque le suivant** : (1) **`bot3_trim`** — 6 Gio *et* −0,46 Gio/j, le seul vrai levier, voir la next action en tête ; (2) **l'armer en timer hebdomadaire**, sans quoi la coupe est unique ; (3) les **~4,3 Gio de caches** (npm 3,2 · journaux 0,8 · pip 0,2 · apt 0,1) ; (4) une **rétention sur `polymarket.db`** — 4,9 Go à 30 j, mais **aucun outil n'existe**, il faudrait la même reconstruction-échange que `bot3_trim`.
  - **Et le terme qu'aucun de ces gestes n'atteint, à regarder en face** : `rtds_ticks` + `book_snapshots` croissent de **0,39 Gio/j — 11,9 Gio par mois — et la spec les dit IRREMPLAÇABLES.** Après tous les gestes ci-dessus, avec ~30 Gio libres, cela fait **environ trois mois**, pas davantage. `report.py` liste lui-même les trois issues restantes : *« keep=1 · backup.dir sur un autre système de fichiers · archive hors machine »*. Les deux premières sont des expédients. **Il n'y a pas d'issue durable sans une archive hors machine — ou le volume qui vient d'être écarté.** Écrit ici pour que la revue ne le redécouvre pas comme une surprise. *(Demande d'Hector du 2026-08-23 : « écris noir sur blanc ce que ça NE règle PAS ».)*
- [ ] 🔴 **La sauvegarde de `bot3.db` est morte depuis le 18/08 — 17,9 Go sans filet.** Elle ne s'est pas cassée : **elle se refuse elle-même**, et elle le dit. `report/backup_refuse — garde d'espace : pic requis 8,49 Gio` puis `** BACKUP REFUSÉ CE MATIN **`, chaque matin à 05:32. Il ne reste que **deux archives, du 17/08 et du 18/08** (446 et 471 Mo), soit 877 Mo dans `bot3_btc5min/data/backups/` — **intouchables, ce sont les seules copies existantes.** Conséquence à énoncer sans détour : depuis cinq jours, une corruption de `bot3.db` ferait perdre `rtds_ticks` et `book_snapshots`, que la spec du projet dit **irremplaçables** (`trades`, lui, est re-téléchargeable depuis data-api). **La garde a raison de refuser — c'est le disque qu'il faut réparer, pas la garde.** *(Trouvé le 2026-08-23.)*
  - **✅ Elle repart TOUTE SEULE, aucune relance manuelle.** Vérifié dans `report.py` : la garde relit `shutil.disk_usage()` **à chaque run**, sur les deux systèmes de fichiers, sans état persistant, sans drapeau, sans verrou. `bot3-report.timer` est resté `enabled` + `active`, prochain tir **lun 2026-08-24 05:30 UTC**. Elle détecte même le changement de FS toute seule — `meme_fs` est recalculé à chaque passage, et le rapport l'imprime : *« FS de la base : le MÊME aujourd'hui — vérifié à chaque run, jamais supposé »*. **Dès que les deux marges passent, la sauvegarde se fait.** Et elle peut être forcée sans attendre 05:30 : `systemctl --user start bot3-report.service` (run réel, ~2 min, envoie le rapport Telegram).
  - **Ce que le filet couvre exactement** : la copie est **sélective** — 48 % du fichier (`cycles`, `rtds_ticks`, `book_snapshots`, `hb_opportunities`, `collector_events`, `report_daily`), et exclut `trades` + `cycle_outcomes` **parce qu'ils se re-téléchargent** depuis data-api / Gamma. Le filet restauré couvre donc précisément la part **irremplaçable**.
- [ ] 🔴 **DISQUE — le VPS sature vers le 2026-08-27, et le dead-man's switch crie juste depuis le 17/08 sans que personne l'entende.** Mesuré le 2026-08-23, **12 662 points relevés dans le journal de `bot3-heartbeat` lui-même** (il enregistre `disk_free` chaque minute) : **17,9 Go libres le 13/08 → 5,3 Go le 23/08**, pente de régression **−1,22 Go/jour** sur 9,86 jours, décroissance régulière sans plateau. **À ce rythme, zéro octet libre dans ~4,3 jours, soit autour du 2026-08-27.** Quand `/` est plein, ce ne sont pas les timers de la veille qui tombent, ce sont **les 21 timers du serveur, `polymarket.db` en écriture comprise** — une base SQLite qui rate une écriture pendant un WAL est le pire moment pour manquer de place.
  - **Le heartbeat n'a jamais menti.** Il rapporte `heartbeat ROUGE — … disk_free: 5.3 G libres (mini 15 G) → /fail` et le POST vers healthchecks.io **répond HTTP 200 « OK » en ~130 ms, chaque minute, depuis six jours**. Le seuil de 15 Go a été franchi vers le **15/08** ; le premier échec systemd est daté du **2026-08-17T01:10**, et il y en a **1 484 en trois jours**. Le mécanisme fonctionne de bout en bout jusqu'à healthchecks.io. **C'est le dernier saut — healthchecks.io vers Hector — qui est muet.** Voir la famille en fin de [[La panne silencieuse]].
  - **Ce que le service rapporte n'est pas ce dont il souffre**, et c'est ce qui a entraîné à l'ignorer : un journal plein d'échecs `timeout` ne dit **nulle part** « le disque se remplit » — il faut ouvrir la sortie du processus pour le voir. ⚠️ *Rectification du 2026-08-23 au soir, après mesure : la première explication écrite ici — « il poste son alerte avec succès puis reste pendu » — était **fausse**. Le run n'est pas pendu, il est **lent** : 43,6 s et 51,6 s chronométrés, contre un `TimeoutStartSec=45` posé **à l'intérieur** de cette plage. Le POST arrive à la **fin** d'un run lent, pas au début d'un run bloqué. Ce n'est donc pas la forme n° 3 de [[La panne silencieuse]] — c'est un délai mal calibré qui **fabrique** du bruit et enterre la forme n° 6. Corrigé, voir plus bas.*
  - **La pente, attribuée par la mesure — et `polymarket.db` n'en fait que 8 %, pas un tiers.** *(Le « tiers » annoncé plus tôt le 23/08 venait d'une fenêtre de 60 s faussée par un checkpoint WAL ; recalculé sur l'historique complet de chaque base, et recoupé par une mesure de 600 s.)*

    | source | volume | ancienneté mesurée | pente | part |
    |---|---|---|---|---|
    | **`bot3_btc5min/data/bot3.db`** | **17,91 Go** | `trades` du 31/07 au 23/08 (22,9 j) | **0,78 Go/j** | **64 %** |
    | Docker / Trigger.dev | 14,10 Go | conteneurs up depuis ~92 j | ~0,15 Go/j | ~13 % |
    | cache npm (`.npm/_cacache`) | 3,40 Go | un blob de ~95 Mo **par jour** | 0,095 Go/j | 8 % |
    | `veillebot/db/polymarket.db` | 7,89 Go | `market_snapshots` du 06/06 au 23/08 (78,2 j) | 0,10 Go/j | 8 % |
    | journaux, `vibe.db`, logs Docker | ~3,2 Go | — | ~0,08 Go/j | ~7 % |

    Somme des pentes : **1,18 Go/j**, contre **1,22 Go/j** mesurés sur le disque. L'attribution est complète.
  - ⚠️ **Chiffre corrigé le 23/08 au soir, après trois mesures** : Docker n'occupait pas 14,1 Go mais **21 Gio** — volumes 14 Gio + **couches d'images 6,5 Gio dans `/var/lib/containerd`** (le driver `overlayfs` ne les range pas sous `/var/lib/docker`) + 517 Mio de journaux de conteneurs. Le 14,1 venait d'un `du -x` qui excluait les montages overlay ; le 19 Gio intermédiaire d'un `du` sans `-x` qui **traversait les overlays montés et comptait deux fois** les couches. **C'est l'arrêt de la pile qui a rendu la mesure honnête**, en démontant les overlays. *Leçon jumelle de celle du disque : ne pas mesurer un système pendant qu'il tourne si la mesure peut compter deux fois.*
  - **La pile a été ARRÊTÉE le 2026-08-23** (étape 1 seule, `stop`, réversible) — arbitrage d'Hector contre l'agrandissement. Elle est en parfaite santé et n'exécutait plus rien du métier depuis le **21/05**. Détail, perte réelle et procédure d'export des variables : [[_autonomous]], à qui la pile appartient.
  - **Trajectoire après l'arrêt complet (étapes 2 à 5)** : `/` passerait à **~26,3 Gio libres**, pente **1,07 Go/j** (1,22 − 0,15). **La sauvegarde `bot3` repart largement** — la garde exige 13,49 Gio, on en aurait 26,3. Mais le mur du backup se referme à **1,44 Gio/j** (l'espace baisse *et* le seuil monte avec `bot3.db`) : **il revient vers le 2026-09-01, l'alarme du heartbeat vers le 03/09, le disque plein vers le 18/09.** Huit jours de sursis, pas une solution — d'où la next action `bot3_trim` en tête.
  - **Réclamable sans toucher à une seule donnée : ~5,0 Go** — cache npm **3,40 Go** (pure repousse), journaux systemd 1,0 Go (dont ~0,8 récupérable), logs de conteneurs Docker 532 Mo, caches pip 204 Mo, cache apt 109 Mo. **Cela porte l'échéance du 27/08 au ~01/09 et ne change pas la pente.**
  - **⚠️ Ce qui N'EST PAS réclamable : les 877 Mo de `bot3_btc5min/data/backups/`** — seules copies existantes, et la sauvegarde se refuse elle-même depuis le 18/08. Voir la next action dédiée en tête.
  - **`bot3_trim.py` existe depuis le 03/08, ses deux conditions sont remplies, et il refuserait quand même — faute de place pour travailler.** Jamais exécuté : aucune unité systemd, zéro occurrence au journal, seul son `.lock` daté du jour de son écriture. Condition 1 (des lignes à couper) : remplie. Condition 2 (`disk_free < 20 Gio`) : remplie. **Mais la garde d'espace calcule `besoin = taille + 5 Gio` = 23,3 Go contre 5,5 Go libres → `REFUS`, exit 3** — *« une reconstruction n'a jamais le droit de mettre en danger ce qu'elle reconstruit »*. **Même après avoir libéré les 5,0 Go de caches (10,5 Go libres), il refuserait encore.** Ce qui le débloquerait : libérer les 14,1 Go de Docker → 24,6 Go, au-dessus du seuil. **Docker est la clé qui ouvre le reste.**
  - **✅ Le défaut du 03/08 est corrigé** — le script porte désormais `if production and not args.dry_run:` et `elif not production: « cible HORS PRODUCTION : aucun service touché »`. Pointer une copie n'arrête plus le vrai collecteur.
  - **🔴 Mais `--dry-run` remplirait le disque** — il contourne les deux gardes et écrit quand même la base neuve. Voir la next action dédiée en tête.
  - **Rétention possible sur `polymarket.db`, si Hector le veut** : `market_snapshots` porte **6 107 165 lignes sur 78 jours** et **presque tout le fichier** ; la matière de décision du bot est minuscule à côté (`positions` 86, `decisions` 2 319, `signals` 199 398, `daily_state` 79). Au-delà de 30 j : 3 815 680 lignes (62 %, ~4,9 Go) ; au-delà de 14 j : 5 056 962 (83 %, ~6,5 Go) ; au-delà de 7 j : 5 581 250 (91 %, ~7,2 Go). **Deux différences avec bot3** : cette base n'a **aucun trigger R5** (rien n'interdit un `DELETE`), et **aucun outil de coupe n'existe** — un `VACUUM` demanderait ~2× la taille du fichier en espace libre, donc il faudrait la même approche reconstruction-échange que `bot3_trim`.
  - **Rien n'a été supprimé** — arbitrage de données, il appartient à Hector (consigne du 2026-08-23). *(Trouvé en marche 2 du chantier [[_veille-ia]], en cherchant tout autre chose.)*
- [x] ~~**`bot3-heartbeat` fabrique 1 484 faux échecs qui enterrent sa propre alerte.**~~ — **corrigé le 2026-08-23** : `TimeoutStartSec` **45 → 120**. Ce n'était pas un blocage — durée réelle mesurée ce jour : **43,6 s et 51,6 s**, le délai était posé **à l'intérieur de la plage de fonctionnement**, et le service perdait la course deux fois sur trois. Le contrat d'alerte le permettait largement : le timer annonce lui-même *« Period 1 min / Grace 14 min côté healthchecks »*. Sauvegarde `.bak-2026-08-23`, md5 `16393e76…` → `f4d460ad…`, `TimeoutStartUSec` effectif vérifié à `2min`. **Résultat sur 6 tirs réels consécutifs : 0 échec, 6 runs terminés** (36 à 47 s). La cause profonde reste le disque : la durée croît avec `bot3.db`.
- [x] ~~Arbitrage Atlas keep / adjust / stop~~ — **TRANCHÉ le 2026-08-03 : KEEP.** Voir Décisions. La boucle est fermée ; elle ne doit plus être comptée « sans réponse ».
- [ ] **Réexamen d'Atlas au 2026-09-30** (post-rentrée) — point de revue posé avec la décision de keep
- [ ] **Bot 3** : la gate d'exécution reste fermée — collecteur record-only, zéro trade

## Décisions
- **2026-08-03 — ATLAS : KEEP DÉCIDÉ.** Décision d'Hector, **pas un report** : la boucle ouverte depuis le verdict du 17/07 est fermée. **Aucune unité n'est arrêtée** — `atlas`, `copy-tracker`, `atlas-report` et le one-shot `atlas-validation-review` restent `enabled`/`active`, la collecte continue d'accumuler de la matière. Les trois granularités de pause préparées le 03/08 ([[01-Projects/hermes-polymarket/mise-en-pause-atlas-procedure|mise-en-pause-atlas-procedure]]) **restent non exécutées**.
  **Motif : coût nul.** Aucune clé privée n'existe sur le serveur (vérifié le 03/08), tout est en `paper` : l'exposition financière est nulle quoi qu'il arrive. *(Précision au motif énoncé : il reste **10 positions ouvertes pour 25,00 USDC en paper**, la plus ancienne depuis le 11/07 — et non zéro. Le fait est corrigé, la conclusion tient : en paper et sans clé, 10 positions ouvertes ne coûtent pas davantage que zéro.)*
  **Ce keep n'est PAS une réhabilitation.** La thèse du copy-trading reste **invalidée par le verdict du 17/07** ; c'est un maintien sans enjeu, pas un feu vert. Point de revue : **réexamen au 2026-09-30**, post-rentrée.
- 2026-07-04 — **Formule d'edge du Bot 1 corrigée** (Σbid − 1).
- 2026-07-17 — **La fenêtre A/B v2 est close par un one-shot programmé d'avance** (`atlas-validation-review`) qui produit des chiffres et ne juge pas : l'arbitrage reste humain.
- 2026-07-30 — **Bot 3 passe à un collecteur record-only** : repo git dédié, heartbeat minute vers healthchecks.io, gate d'exécution fermée, **zéro trade**.
- 2026-08-02 — **Une sonde externe en lecture seule** (`hermes-collector`, [[_dream-console]]) lit Hermes sans jamais y écrire ; elle a relevé le gel de `state.db` depuis le 13/07 et le défaut de finalisation du 14/07.

## Ressources liées
- [[01-Projects/hermes-polymarket/plan-agrandissement-volume-hetzner|plan-agrandissement-volume-hetzner]] — la séquence exacte commande par commande, **préparée le 23/08, non exécutée** : deux points de non-retour balisés, chaque phase a son inverse
- [[01-Projects/hermes-polymarket/mise-en-pause-atlas-procedure|mise-en-pause-atlas-procedure]] — préparée le 03/08, **non exécutée** : Hector arbitre A/B/C
- [[01-Projects/hermes-polymarket/fiche-technique|fiche-technique]] — bots, cadences systemd, cockpit, pointeurs VPS
- [[01-Projects/hermes-polymarket/decisions-et-lecons|decisions-et-lecons]] — décisions d'architecture et leçons durcies
- **Satellite** : Trading-Bots-Vault — vault Obsidian séparé des bots (`~/Downloads/Trading-Bots-Vault` sur le Mac ; décision du 03/07 : import éventuel en Phase 3)

## Journal du projet
### 2026-07-03
- Note pivot créée (Phase 1 du second brain).

### 2026-07-12
- Import VPS via [[documenter-un-projet]] (ssh lecture seule, user veillebot — aucun fichier modifié sur le serveur). À noter : la revue de fin de validation paper d'Atlas (fenêtre A/B v2) est programmée par timer one-shot le **2026-07-17 18:30 UTC**. Log : `AI/logs/2026-07-12-session.md` (session 3).

### 2026-07-30
- Refresh post-verdict Atlas via ssh **lecture seule** (user veillebot ; aucune écriture, aucun `.env` lu). Lu : unité + journal du one-shot `atlas-validation-review` (verdict A/B du 17/07), `systemctl --user list-timers`, `git log ~/bot3_btc5min`. Statut mis à jour (instantané « Au 2026-07-30 ») ; leçon « winrate ≠ edge » ajoutée à [[decisions-et-lecons]]. Log : `AI/logs/2026-07-30-session.md`.
