---
type: project
statut: actif
tags: [ia, agents, automation]
cree: 2026-08-19
---

# Veille IA (Hermes VeilleBot)

## Objectif

Une veille IA hebdomadaire **entièrement automatisée** : chaque lundi 7 h, le VPS scrape des sources whitelistées, filtre au LLM, et pousse le meilleur dans le notebook NotebookLM **« Veille IA Hector — Q3 2026 (Pro) »** (id `9d6812e4-4764-4ad6-9d3d-f335c7f4dca0`, compte Google `ndrcn937@gmail.com`), avec récap Telegram. En service depuis le 2026-06-03 ; pivot créé le 2026-08-19 à l'issue de la reconnaissance NotebookLM (P4).

## La chaîne complète

```
veillebot-weekly.timer (systemd user veillebot — lundi 07:00 Europe/Paris, Persistent=true)
  └─ ~/veillebot/scripts/main.py            (python du venv ~/notebooklm-veillebot/venv)
       1. sources.yaml      → quoi suivre (chaînes YouTube FR/EN, queries + repos GitHub)
       2. scrapers.py       → YouTube via Apify ; GitHub via API directe (GITHUB_TOKEN, gratuit)
       3. state_db.py       → dédoublonnage SQLite (~/veillebot/state.db, hash d'URL)
       4. filter_kimi.py    → scoring 0-10 par Kimi K2.5 (OpenRouter), seuil 7 ; les
                              focus_topics du yaml nourrissent le prompt du filtre
       5. curator_kimi.py   → sélection top 15 + résumés + tags
       6. notebooklm_pusher → push via le CLI notebooklm-py (NON officiel, v0.6.0, auth
                              cookies — maintenue en vie par le run hebdo lui-même ;
                              alerte Telegram dédiée si elle expire)
       7. telegram_notify   → récap du run
  └─ ~/veillebot/logs/YYYY-MM-DD.json       (un log JSON par run : compteurs + items poussés)
```

Secrets dans `~/.hermes/.env` (Apify, OpenRouter, GitHub, Telegram). Twitter : section présente dans le yaml mais **le code est commenté** dans `scrapers.py` — la réactiver demande les deux. Le venv est partagé avec les timers Hermes — « un seul environnement à maintenir, un seul point de casse » (cf. [[_hermes-polymarket]]).

## Source de vérité hors NotebookLM

**Le notebook est un débouché, pas la mémoire.** Tout ce qui y entre est reconstructible depuis le VPS :
- `sources.yaml` — quoi suivre (versionné par sauvegardes datées `sources.yaml.bak-YYYY-MM-DD`) ;
- `state.db` — tout ce qui a été vu, avec score Kimi, tags et `notebooklm_source_id` des items poussés ;
- `logs/*.json` — chaque run : compteurs et liste titre/URL/score/tags des items poussés.

Si NotebookLM casse (API non officielle, cookies), on perd le chat et les artefacts — **pas la veille**.

## Statut

**Au 2026-08-23 au soir — CHAÎNE RÉPARÉE ET VÉRIFIÉE PAR UN RUN COMPLET.** Les trois marches sont faites : la mémoire (le « vu » n'est plus écrit au scrape), la version (CLI en **0.8.1**), l'authentification (voie A — cookies importés du Mac + keepalive systemd toutes les 15 min avec `--verify`, alerte Telegram sur `OnFailure` testée de bout en bout). Run du soir : **140 scrapés → 80 sans verdict → 17 filtrés → 14 curés → 14 poussés, 0 échec, 664 s**. **Le notebook passe de 79 à 93 sources.** `state.db` : 583 items, **89 poussés**, 184 avec verdict, 399 sans verdict (dont 40 non jugés par panne Kimi, qui repasseront). Timer **réarmé**, prochain tir lundi 24/08 05:00 UTC, `TimeoutStartSec` porté de 900 à 1800 s sur mesures. **Le run sort en `exit 1` volontairement** : l'escalade `kimi_scoring_unjudged` met l'unité en échec quand Kimi n'a pas jugé — ce n'est pas une panne, c'est le cri qu'on a câblé. Les instantanés antérieurs sont conservés au-dessous.

**Au 2026-08-23 (matin) — chaîne à l'arrêt : le push NotebookLM est mort, timer désactivé avant le run du 24/08.** Google a renommé NotebookLM en « Gemini Notebook » et migré sur `notebook.google.com` ; `notebooklm.google.com` renvoie désormais un **301 permanent**. Le CLI du VPS est en **0.6.0** (publiée le 2026-05-29) : **27 occurrences de l'ancien host codées en dur dans le paquet installé, 0 du nouveau**. Mesuré sur le serveur le 23/08 en lecture seule : `Token fetch ✗ fail`, `notebooklm list` → « Authentication expired or invalid ». **Ce n'est pas l'expiration des cookies** — sur le Mac, des cookies frais obtenus par login navigateur le 23/08 à 12:41 (63 cookies, valides jusqu'en 2027) échouent identiquement en 0.3.4. Se reconnecter ne répare rien ; seule la version le peut — correctif upstream en **0.8.0** (2026-08-03, bascule du host par défaut) puis **0.8.1** (2026-08-14, reconnaissance du host après le rebrand). ⚠️ **CORRIGÉ le 2026-08-23 au soir (marche 3) : « seule la version le peut » est faux, et l'expérience qui le fondait était biaisée.** Le VPS **en 0.8.1**, avec ses propres cookies, rend toujours `Token fetch ✗ fail` — **la version était nécessaire, pas suffisante : les deux causes étaient empilées.** Le test du Mac changeait **deux variables à la fois** (0.3.4→0.8.1 *et* cookies fraîchement reloggés) ; il prouvait que la version était *une* cause, jamais qu'elle était la seule. Voir Décisions. Le timer `veillebot-weekly` a été **stoppé et désactivé le 23/08** (voir Décisions). Dernier run sain : **17/08** — 94 scrapés → 31 nouveaux → 8 filtrés → 5 curés → **5 poussés**, `push_failed: 0`, `errors: []`. `state.db` au 23/08 : 458 items, 75 poussés. L'instantané du 19/08 est conservé au-dessous.

**Au 2026-08-19 — chaîne saine, v1.1 du yaml déployée ce jour.** 12 runs depuis le 04/06, 0 erreur, 75 items poussés ; dernier run le 17/08 (94 scrapés → 31 nouveaux → 8 filtrés → 5 poussés, 7 min 33). Le notebook contient 79 sources (75 pipeline + 4 manuelles). Corrections du 19/08 appliquées sur le VPS et validées à blanc (queries testées contre la vraie API GitHub — 10 repos rendus par chacune des 5 nouvelles ; releases speech-to-speech captées : v0.2.10 → v0.2.12 ; zéro Apify ni Kimi dépensé au test). Premier run en conditions réelles : **lundi 24/08**. Le soir du 19/08, le `SYSTEM_PROMPT` du filtre a été aligné à son tour (voir Décisions) : la v1.1 partira lundi avec un profil d'août, plus de juin.

## Next actions

- [x] ~~🔴 **LE CHANTIER RÉEL — `mark_seen` doit se déplacer après le push, ou `pushed_to_notebooklm` doit entrer dans le filtre.** Ce n'est pas la version, c'est la conception : **même en 0.8.1, la prochaine panne de push brûlera silencieusement une cohorte entière.** `main.py` appelle `mark_seen()` au **STEP 3 (ligne 118)**, sur chaque item scrapé, bien avant `push_curated_items()` au **STEP 6 (ligne 162)**. Le branchement `auth_expired` (ligne 178) ne fait que journaliser et alerter — **aucun rollback, aucun démarquage, aucun `DELETE` dans tout le fichier**. Et le filtre du run suivant ne consulte jamais l'état de push : `is_seen()` et `filter_new_urls()` testent la seule existence de la ligne. Conséquence : un item curé mais non poussé est **exclu définitivement**, et comme `kimi_score` et `tags` ne sont écrits que par `mark_pushed()` (donc au succès seul), **il devient indiscernable d'un item recalé au scoring**. Les deux corrections possibles sont dans le titre ; la seconde est la moins invasive. *(Découvert le 2026-08-23.)*~~ — **corrigé et déployé le 2026-08-23** (marche 1). Ni l'une ni l'autre des deux voies telles qu'écrites : `is_seen()` et `filter_new_urls()` **n'étaient appelées par personne**, la vraie porte était le booléen de `mark_seen()` — l'option 2 littérale était un no-op. Retenu : **la porte teste désormais le VERDICT et non l'observation** (`is_resolved()`), et chaque étape enregistre le sien (`mark_resolved()` au filtre et au curateur, `mark_pushed()` au push). 42 assertions sur une copie de `state.db`. Voir Décisions.
- [ ] 🔴 **Le correctif complet : rejouer les lignes non résolues depuis `state.db` au STEP 6, sans repasser par Kimi.** Le correctif du 23/08 empêche l'oubli, il ne garantit pas le rattrapage : **la récupération passe par le re-scrape.** Un item perdu *et* sorti de la fenêtre de scrape — une vidéo YouTube tombée hors de la liste récente, un repo passé sous le radar d'une query — ne reviendra jamais tout seul, même marqué « sans verdict ». Ce qu'il faut : au STEP 6, avant les items du run, **relire les lignes `pushed_to_notebooklm = 0 AND kimi_score IS NULL` et pousser leur URL directement** — `push_source()` ne demande que l'URL, donc **zéro coût Kimi, zéro dépendance au scrape**. Deux points à trancher le jour venu : borner l'ancienneté rejouée (sinon la file grossit indéfiniment), et distinguer « curé, non poussé » de « scrapé, jamais jugé » — les deux portent `kimi_score NULL` aujourd'hui, ce qui demande soit une colonne, soit un marqueur au STEP 5. *(Identifié le 2026-08-23 pendant la marche 1, laissé hors périmètre à dessein — demande d'Hector : « qu'il ne se perde pas ».)*
- [ ] **Réactiver le timer — SEULEMENT quand la chaîne est réparée.** Commande exacte : `systemctl --user enable --now veillebot-weekly.timer` (user `veillebot`). ⚠️ **`Persistent=true` : systemd rattrapera le tir manqué IMMÉDIATEMENT à la réactivation, il n'attendra pas le lundi suivant.** Réactiver = déclencher un run dans la minute. Ne pas le faire avant que le push fonctionne, sous peine de reproduire exactement la perte qu'on vient d'éviter.
- [x] ~~**Réparer le push : monter le CLI de 0.6.0 à 0.8.1 sur le VPS** — avec deux précautions. (1) Le venv `notebooklm-veillebot/venv` est **partagé avec les timers Hermes** (« un seul environnement à maintenir, un seul point de casse ») : la montée les touche aussi, à tester à blanc. (2) La version seule ne suffira pas — il faudra ré-authentifier, et sur un VPS headless cela passe par le **master-token headless auth** introduit en 0.8.0. Dispositif neuf, à configurer, pas un `pip install`.~~ — **précaution (1) levée et montée faite le 2026-08-23** (marche 2) : `pip freeze` diffère d'**exactement une ligne**, 33 paquets avant comme après. **La précaution (2) reste ouverte, c'est la marche 3** — mais elle est peut-être caduque : voir Décisions.
- [x] **AUTHENTIFICATION — ✅ RÉPARÉE le 2026-08-23 : voie A retenue par Hector, configurée et prouvée.** `Token fetch ✓ pass`, exit 0, **5 notebooks visibles** depuis le VPS. Motif de l'arbitrage, ses mots : « un master token est une clé permanente de tout mon compte Google sur un serveur qui fait tourner des bots ». Cookies importés depuis le Mac (`auth import-cookies`, 52 retenus), keepalive `notebooklm-keepalive.timer` toutes les 15 min avec `--verify`, alerte Telegram câblée sur `OnFailure` et **testée de bout en bout**. Détail en Décisions. L'analyse qui a mené là est conservée ci-dessous.
  - *(analyse du 23/08 — conservée)* La session Google du VPS était **morte côté serveur** : en 0.8.1, tous les contrôles structurels passent (33 cookies chargés, `SID`, `__Secure-1PSID/3PSID`, `1PSIDTS` valide jusqu'au 2027-04-14, `OSID` sur `notebook.google.com`) et Google répond quand même `Token fetch failed: Authentication expired or invalid. Final URL: https://accounts.google.com/…`. Le bocal est parfait, la session derrière est morte.
  - **Voie A — cookies importés + keepalive programmé (recommandée).** `notebooklm auth import-cookies` accepte un JSON exporté depuis le Mac, où le login navigateur fonctionne ; puis un timer systemd lance `notebooklm auth refresh --verify` **toutes les 15-20 min** — cadence **écrite dans l'aide du CLI**, qui précise que la commande est « designed to be scheduled by the OS (launchd / systemd / cron) so that an otherwise-idle profile does not stale out ». `--verify` fait un token fetch passif après le refresh et **sort non-zéro si les cookies rafraîchis n'authentifient pas réellement** — c'est le contrôle de bout en bout que réclame la 6ᵉ forme de [[La panne silencieuse]]. **Zéro paquet nouveau, zéro credential durable sur le serveur.**
  - **Voie B — master token headless.** `notebooklm login --master-token --account EMAIL` échange un `oauth_token` à usage unique contre un **master token Android GMS durable** (`aas_et/`, via `gpsoauth`, signature d'app `com.google.android.apps.chromecast.app`), écrit en `master_token.json` mode 0600 à côté de `storage_state.json` ; ensuite le CLI **frappe des cookies web à la demande, sans navigateur, indéfiniment**. Coût dans le venv partagé, mesuré à blanc : **exactement 2 paquets neufs** (`gpsoauth` 2.0.0, `pycryptodomex` 3.23.0), aucune dépendance partagée touchée. Le bootstrap réclame un `oauth_token` capturé sur une machine à navigateur (`--oauth-token`) ou via `--cdp-url`.
  - **Ce qui départage n'est pas la technique, c'est le pouvoir accordé.** Un master token est un credential **durable, sans date d'expiration, révocable seulement par changement de mot de passe ou retrait de l'« appareil »** du compte Google — et il ouvre `ndrcn937@gmail.com` en entier, pas seulement le notebook. La voie A n'accorde que des cookies de session. La voie B pose une clé permanente du compte sur un serveur qui héberge aussi 21 timers.
  - **Et la voie A traite la cause, pas le symptôme** : la session est morte **parce que rien ne l'a maintenue en vie**. Le pivot dit depuis le début « auth maintenue en vie par le run hebdo lui-même » — un rafraîchissement **hebdomadaire** contre une cadence documentée de **15-20 minutes**. Ça a tenu onze semaines par chance, pas par conception. Un master token permettrait de re-frapper après chaque mort ; un keepalive empêche la mort. *(Les deux ne s'excluent pas : A comme conception, B comme voie de secours si l'on veut zéro intervention humaine à jamais.)*
- [ ] **Le venv « partagé » l'est moins qu'on ne croyait — et c'est vérifiable, pas à supposer.** Ce sont **10 services** qui l'utilisent, pas 20 : les 7 `polymarket-*`, `hermes-freeze-detector`, `hermes-morning-brief`, et `veillebot-weekly`. Les timers `alternance-*`, `lba-fetch` et `perplexity-fetch` tournent sur `/usr/bin/python3` nu ; les `bot3-*` ont leur propre venv (`~/bot3_btc5min/venv`) ; `hermes-gateway` a le sien (`~/.hermes/hermes-agent/venv`). **Et un quatrième venv dort dans `~/veillebot/venv`** (Python 3.11 via uv, 82 Mo, openai + apify, sans playwright ni notebooklm) — l'unité systemd ne l'utilise pas. À arbitrer un jour : le laisser, ou l'archiver. *(Relevé le 2026-08-23.)*
- [x] ~~**Lundi 24/08 — vérifier le récap Telegram du premier run v1.1** : les nouvelles queries injectent ~50 candidats d'un coup (rattrapage d'historique voulu), le plafond `max_items_per_run: 15` fait le tri.~~ — **caduque : le run du 24/08 n'aura pas lieu**, timer désactivé le 23/08. Le rattrapage d'historique reste intact et sera injecté au premier run après réparation. *(À noter : le one-shot launchd `com.hectorvolant.secondbrain.veille-rapport-2408` sur le Mac, lui, tirera bien le 24/08 à 20 h 04 — il trouvera un run absent, puis s'auto-désinstallera comme prévu. Le mécanisme de rapport n'existera donc plus lors de la réactivation.)*
- [x] ~~**Surveiller sur 3 runs si les items voix passent le filtre** : les focus_topics sont alignés, mais le `SYSTEM_PROMPT` de `filter_kimi.py` décrit encore un Hector sans jarvis-voice ni ads-studio (« LegitVision, Vivo, Autonomous ») — s'il massacre la voix au scoring, c'est lui qu'il faudra mettre à jour (décision à part, non prise le 19/08).~~ — **tranché le 2026-08-19 au soir, sans attendre les runs : le `SYSTEM_PROMPT` est mis à jour le jour même.** « Attendre 3 runs pour confirmer ce qu'on sait déjà, c'est trois semaines de veille mal filtrée avant lundi. » Voir Décisions ; la preuve reste due au run du 24/08.
- [ ] **2026-09-07 — revue Polymarket** (clause écrite dans le yaml) : si 0 item retenu en 3 runs, retirer la query **et** les focus_topics « polymarket » / « prediction market ».
- [ ] **2026-11-01 — revue du profil du filtre** (`SYSTEM_PROMPT` de `filter_kimi.py`), **ou dès qu'un nouveau chantier majeur démarre** : ce prompt a tenu deux mois et demi sans relecture alors qu'il décide de tout ce qui entre dans la veille — sinon on redécouvre le même décalage en novembre. *(Ajoutée le 2026-08-19, demande d'Hector.)*
- [ ] (option) Réinvestir le budget Apify libéré par Instagram — voir décision du 19/08 ci-dessous.

## Décisions

- **2026-08-23 — Timer coupé plutôt que CLI mis à jour, à quinze heures du run.** Arbitrage d'Hector : « une montée en 0.8.1 à quinze heures du run, sans test, c'est le genre de geste qui casse deux systèmes au lieu d'un. Un run décalé d'une semaine ne coûte rien. » Le venv est partagé avec les timers Hermes. Couper le timer arrête la chaîne **avant l'étape 3** : zéro budget Apify, zéro OpenRouter, zéro ligne écrite dans `state.db`, rien à récupérer ensuite. C'est le seul geste qui laisse l'état intact.
- **2026-08-23 — `stop` + `disable`, et non `mask` : systemd a refusé le masquage.** `systemctl --user mask` écrit son lien vers `/dev/null` dans `~/.config/systemd/user/` — exactement là où vit l'unité. Refus : « File /home/veillebot/.config/systemd/user/veillebot-weekly.timer already exists. » **Le fichier d'unité a survécu intact** (md5 `95b2ec9c…` identique avant et après, 560 octets, mtime Jun 4 08:54 — vérifié dans les deux sens). Retenu à la place : `stop` puis `disable`, qui retire le lien de `timers.target.wants/`. État vérifié : `is-enabled: disabled`, `is-active: inactive`, absent de `list-timers --all`, et aucune autre unité ne déclenche `veillebot-weekly.service`. État antérieur à restaurer : `enabled` + `active`.
- **2026-08-23 — Le vrai défaut n'est pas la version, c'est l'ordre d'écriture — et il aurait survécu à la mise à jour.** C'est la leçon de la session : on a cherché une panne d'authentification, trouvé un renommage de domaine, et découvert au passage un défaut de conception qui, lui, était là depuis le 03/06 et n'attendait qu'une panne de push pour se manifester. **Un dédoublonnage qui marque « vu » avant de savoir si l'item est arrivé transforme chaque incident de push en perte silencieuse.** Le schéma `seen_items` distingue pourtant bien `first_seen` de `pushed_to_notebooklm` — la colonne existe, elle n'est simplement jamais consultée pour décider ce qui est nouveau. Voir la next action en tête de liste.
- **2026-08-23 — « La sortie n'est pas la réponse », deux fois dans la même session.** `notebooklm source list --json` a rendu `{"sources": []}` avec un **code de sortie 0** sur une session expirée : sans passage en sortie brute, on concluait « notebook vide ». Et `notebooklm profile list` affichait `authenticated` sur un `storage_state.json` du 3 juin — les quatre contrôles qui passent (fichier existe, JSON valide, cookies présents, cookie SID) sont structurels ; **seul `auth check --test` touche le réseau, et c'est la seule ligne qui fait foi**. À se rappeler pour tout contrôle de cette chaîne.

- **2026-08-23 (soir) — LE CORRECTIF DE LA MARCHE 1 EST VALIDÉ EN PRODUCTION, et c'est une interruption réelle qui l'a prouvé.** Timer réarmé sur GO d'Hector, run déclenché à 16:58:51. **Tué par systemd à 17:14:09**, `TimeoutStartSec=900` dépassé — une seconde après le début du STEP 5, avant toute curation et tout push. Et c'est précisément ce qui rend la preuve solide : **le run est mort exactement là où l'ancien code aurait perdu une cohorte entière.**

    | `state.db` | avant | après |
    |---|---|---|
    | total | 458 | **545** (+87 lignes neuves) |
    | poussés | 75 | **75 — inchangé** |
    | avec verdict | 75 | **144** (+69) |
    | sans verdict → repasseront | 383 | **401** |
    | **verdicts ≥ 7 non poussés** | — | **0** |

  **La dernière ligne est toute la démonstration.** 130 items sont passés au scoring : **31 ont franchi le seuil, 69 ont été recalés et ont reçu leur verdict, 30 n'ont rien reçu parce que leur batch Kimi a échoué** (31 + 69 + 30 = 130). Les 31 qui méritaient le notebook n'ont **aucun** verdict écrit : ils reviendront. **Sous l'ancien code, `mark_seen` les aurait marqués « vus » au STEP 3, le run serait mort au même endroit, et ils seraient devenus indiscernables des recalés** — exactement les 37 items retrouvés le matin même dans les logs de juin à août. Le notebook est resté à 79 sources, aucun Telegram (mort avant le STEP 7), aucun log JSON (le `finally` ne survit pas à SIGTERM).
  - **Les deux autres ajouts de la marche 1 ont tenu aussi, sur le même run** : `kimi_ok` a empêché d'enregistrer 30 faux verdicts à 0,0 — et l'escalade a crié, `[ERROR] 30 items NON jugés (batch Kimi en échec) — ils repasseront`.
  - **Et les 30 non jugés et le dépassement du délai sont le MÊME problème** : trois batches Kimi ont expiré à 120 s chacun — visible dans les trous du journal (batch 4→5 : 153 s ; batch 10→11 : 201 s). Ce sont ces ~360 s perdues qui ont poussé le run au-delà des 900 s.
  - **`TimeoutStartSec` porté de 900 à 1800 s**, avec les mesures écrites dans le commentaire de l'unité pour que le prochain lecteur sache sur quoi il est calibré : **452 s** le 17/08 (pipeline complet nominal), **918 s** le 23/08 (scrape + scoring de 130 items dont 3 batches en échec), **~1250 s** pour un run complet équivalent. Sauvegarde `.bak-2026-08-23`, md5 `95a66cf6…` → `4235bd95…`, valeur effective vérifiée à `30min`. **Troisième délai de la journée calibré sur un régime périmé** — voir la 7ᵉ forme de [[La panne silencieuse]].
  - **✅ Et le run relancé est allé au bout : 14 items poussés, le notebook passe de 79 à 93 sources.** `2026-08-23 17:19:16 → 17:30:20`, **664 s**. Chaîne complète, les 7 étapes : 140 scrapés → **80 sans verdict** (contre 130 au run précédent — les 69 verdicts écrits ont fait leur travail) → 17 ≥ 7,0 → **23 recalés et marqués jugés** → 14 curés (**3 écartés par le curateur, marqués jugés eux aussi**) → **14 poussés, 0 échec** → récap Telegram en 2 messages. Compteurs cohérents au dernier item : 23 + 40 + 17 = 80, et 75 + 14 = **89 poussés** en base.
    - **`exit 1` — et c'est l'escalade qui crie, pas un échec.** `errors: ['kimi_scoring_unjudged: 40']`. La demande d'Hector — *« je veux que ça crie »* — se lit maintenant à trois endroits : le journal en `[ERROR]`, le code de sortie qui met l'unité en échec systemd, et **le récap Telegram** qui affiche `⚠️ 40 items NON jugés (panne de scoring Kimi)` sous un bloc `🚨 Anomalies`. Avant ce jour, ce run aurait envoyé un message strictement identique à un run sain.
    - **Le lien du récap pointe bien sur `notebook.google.com`** — le host en dur corrigé le matin est vérifié en production.
    - ⚠️ **À surveiller, et ce n'est pas notre code : Kimi a échoué sur 40 items sur 80** (50 %), après 30 sur 130 au run précédent. Ces 40 n'ont **aucun verdict** et repasseront — c'est exactement le comportement voulu, mais deux runs de suite avec la moitié du lot non jugée, c'est un signal côté OpenRouter, pas côté pipeline.
  - **Le rattrapage `Persistent=true` n'a pas eu lieu, et l'avertissement répété toute la journée ne s'appliquait pas.** `OnCalendar=Mon 07:00` : dernier tir lundi **17/08**, prochain lundi **24/08**, et le timer a été coupé onze heures **un dimanche**. Aucun lundi manqué, donc rien à rattraper. L'avertissement reste vrai en général — il ne l'était pas ce jour-là. *Un avertissement juste peut être hors sujet : celui-ci n'était pas faux, il était mal daté.*

- **2026-08-23 (marche 3, fin) — Voie A : la session est réamorcée depuis le Mac, et un keepalive l'empêche de remourir.** `Token fetch ✓ pass`, exit 0, **5 notebooks** depuis le VPS. **Arbitrage d'Hector, et il porte sur le pouvoir accordé, pas sur la technique** : « un master token est une clé permanente de tout mon compte Google sur un serveur qui fait tourner des bots ». Le master token headless reste possible et documenté ; il n'est pas installé (`gpsoauth`/`pycryptodomex` absents, 33 paquets au venv, inchangé).
  - **Ce qui a été fait** : `storage_state.json` du Mac transféré par `scp` en 600 puis `notebooklm auth import-cookies` (**52 cookies retenus**, l'ancien profil sauvegardé deux fois — `profil-avant-auth/` et le `.bak` du CLI), fichier de graine **détruit au `shred`** juste après. Différence structurelle notée entre les deux bocaux : le cookie SID du Mac est porté par **`.notebook.google.com`** (nouveau host), celui du VPS l'était par `.notebooklm.google.com`.
  - **Le keepalive** : `notebooklm-keepalive.timer`, `OnCalendar=*:0/15`, `Persistent=false` (« la fraîcheur ne se rattrape pas », convention reprise du timer de `bot3-heartbeat`), `RandomizedDelaySec=30`. Le service lance `auth refresh --verify` — **le `--verify` n'est pas décoratif** : l'aide du CLI dit qu'*« a successful refresh command alone does not prove the post-refresh cookies work »*, donc sans lui on fabriquerait exactement la panne qu'on répare. **`TimeoutStartSec=60` choisi sur mesure** (durée réelle 1,03 s et 1,75 s), pas au jugé — leçon du `bot3-heartbeat` appliquée le jour même.
  - **L'échec du keepalive atteint Hector, et c'est prouvé, pas supposé.** Ce serveur n'avait **aucun `OnFailure`** : un timer en échec ne se voyait que dans `systemctl --failed`. Ajouté : `notebooklm-keepalive-failed.service` → `scripts/keepalive_alert.py` → Telegram, avec **étranglement à une alerte par 6 h** (sinon 96 messages par jour, et *un test qui crie à chaque usage nominal est un test qu'on désarme*), témoin **effacé par `ExecStartPost=` à chaque succès** pour qu'une rechute réalerte aussitôt. **Testé de bout en bout par un drop-in temporaire forçant l'échec** : `alerte Telegram envoyee` à 14:06:18, étranglement confirmé au second appel, drop-in retiré, nominal revérifié. C'est la parade de la 6ᵉ forme de [[La panne silencieuse]] appliquée à elle-même — *forcer une alerte réelle et confirmer qu'un humain la reçoit.*
  - **La conséquence à connaître** : le bocal du Mac et celui du VPS sont désormais **des clones de la même session Google**, et seul le VPS la rafraîchit toutes les 15 min. Si Hector veut garder un CLI NotebookLM indépendant sur le Mac, un `notebooklm login --fresh` là-bas lui donnera son propre bocal sans toucher au VPS.

- **2026-08-23 (marche 3) — L'expérience du matin était biaisée : deux causes étaient empilées, et le Mac n'en a prouvé qu'une.** `auth check --test` sur le VPS **en 0.8.1** : `Token fetch ✗ fail`, exit 1. L'hypothèse de fin de marche 2 — « les 62 cookies du VPS couvrent déjà `notebook.google.com`, donc ça devrait passer comme sur le Mac » — est **réfutée**. Le diagnostic `--json --passive` est sans ambiguïté : `storage_exists ✓`, `json_valid ✓`, `cookies_present ✓` (33 chargés sur 62, le reste écarté par l'allowlist de domaines du CLI), `sid_cookie ✓`, `__Secure-1PSIDTS` valide **jusqu'au 2027-04-14** — et `token_fetch ✗` avec `Final URL: https://accounts.google.com/…`. **Le bocal à cookies est structurellement parfait et la session derrière est morte côté serveur.**
  - **La leçon de méthode, et elle vaut au-delà de ce chantier : le test du Mac changeait DEUX variables à la fois.** Version `0.3.4 → 0.8.1` **et** cookies fraîchement reloggés à 12:41. Il prouvait que la version était *une* cause ; il ne pouvait pas prouver qu'elle était la *seule*, parce que la fraîcheur des cookies masquait la seconde. Le VPS tient la version constante à 0.8.1 avec ses propres cookies et échoue : **les deux étaient cassés.** La phrase « se reconnecter ne répare rien ; seule la version le peut » est corrigée en tête du `## Statut`. *Une expérience à deux variables ne tranche jamais qu'entre « au moins une » et « aucune ».*
  - **Non prouvé, et noté comme tel** : le login navigateur d'Hector sur le Mac le 23/08 à 12:41 a **peut-être** fait tourner la session côté Google et invalidé la copie du VPS. Rien dans ce qu'on peut lire depuis le serveur ne permet de le confirmer — l'autre explication, le simple vieillissement d'un profil que plus rien ne rafraîchissait, suffit aussi.
  - **La cause profonde était écrite dans le pivot depuis le premier jour** : « auth cookies — maintenue en vie **par le run hebdo lui-même** ». L'aide de `notebooklm auth refresh` en 0.8.1 donne la cadence attendue : **15 à 20 minutes**, « significantly looser may cross the SIDTS server-side validity window ». La veille rafraîchissait **une fois par semaine**. Elle a tenu onze semaines — **par chance, pas par conception**.
  - **Aucune écriture, aucune configuration.** Profil sauvegardé **avant** toute commande d'auth (`backup-2026-08-23/profil-avant-auth/`, md5 vérifiés, `RESTAURER-PROFIL.txt`). Et la sauvegarde n'a même pas servi : `storage_state.json` est resté à `b91d9b4c…`, 62 cookies, mtime `2026-08-19 09:33:51` — **la 0.8.1 n'élague le bocal qu'au succès**, l'échec survient avant toute rotation. Deux voies de réparation mesurées et posées en next action ; **rien n'est installé, `[headless]` n'est pas dans le venv.**

- **2026-08-23 (marche 2) — CLI monté en 0.8.1, et la peur du venv partagé s'est dissoute à la mesure.** `pip install --dry-run --report` avant tout geste : la résolution ne touchait **qu'un seul paquet**, `notebooklm-py` — `filelock 3.29.0` était déjà là et satisfait la 0.8.1, contrairement au Mac qui venait de 0.3.4. Deuxième mesure : **aucun des 9 scripts Hermes du venv ne contient la chaîne `notebooklm`** (les fichiers qui remontaient au grep large sont des unités systemd et des wrappers shell référençant le *chemin* du venv, pas le paquet). **Donc ni changement de dépendance, ni import à casser : le rayon de souffle était nul, et c'est mesuré, pas espéré.** Résultat : `pip freeze` diffère d'exactement une ligne, 33 paquets avant comme après. Version `0.8.1 (01c419a0)` — même empreinte de build que le Mac. Host basculé dans le paquet : 0 → **24 occurrences** de `notebook.google.com` ; les 30 restantes de l'ancien host sont **délibérées** (`PERSONAL_LEGACY_HOST` dans `_env.py`, « still served »), pas un oubli. Sauvegarde `~/notebooklm-veillebot/backup-2026-08-23/` (freeze avant/après, version, `ROLLBACK.txt`).
  - **La vérification Hermes n'est pas « systemctl dit actif » — exigence d'Hector.** Trois capacités réelles exercées en mode sans effet de bord, avant **et** après : `freeze_detector.py --dry-run`, `morning_brief.py --dry-run`, `position_monitor.py --dry-run` — `exit 0`, **même nombre de lignes et même verdict** dans les deux passes. Plus 13/13 dépendances tierces importables, 9/9 scripts compilables, et le listener long-running `process_callbacks.py --watch` **survivant avec le même PID**. Puis les **vrais tirs** observés au journal pendant 8 minutes : dispatcher ×8, executor ×4, brain ×2, monitor ×2, snapshot ×1, tous `Finished`.
  - **La place nécessaire était de 10 Mo, pas de gigaoctets — donc aucun ménage n'était « besoin ».** Roue 1,54 Mo, paquet installé 72 Ko en 0.6.0. Contre 5,3 Go libres. Le ménage n'a **pas** été fait : rien ne le justifiait pour la montée, et purger les journaux systemd d'un serveur de production sans demande n'est pas un geste d'agent.
  - **Ce que la montée a fait apparaître et qui compte plus qu'elle : le disque est une panne active, datée, avec une échéance.** `bot3-heartbeat` — le dead-man's switch — **échoue en boucle depuis le 2026-08-17T01:10** (1 484 échecs en 3 jours) et rapporte `ROUGE … disk_free: 5.3 G libres (mini 15 G) → /fail`. Ce n'est pas la montée : le service tourne sur `~/bot3_btc5min/venv`, **intact** (aucun fichier modifié depuis midi), et l'échec précède de six jours. **Le disque se vide à −1,22 Go/jour et sature vers le 2026-08-27**, mesuré par régression sur **12 662 relevés** que le heartbeat lui-même journalise chaque minute (17,9 Go le 13/08 → 5,3 Go le 23/08, 9,86 jours). `~/bot3_btc5min` pèse 18 Go, `polymarket.db` 7,4 Go. Réclamable sans toucher aux données : ~4,5 Go — **cela repousse au 31/08 et ne change pas la pente.** C'est un arbitrage de données, donc d'Hector. Chiffres complets et famille d'alerte en tête des next actions de [[_hermes-polymarket]].
  - **Erreur de méthode corrigée en séance, elle vaut d'être gardée** : la première estimation de cette session annonçait « 0,43 Go/jour → ~12 jours », extrapolée d'une mesure de **60 secondes** sur le seul `polymarket.db`. Elle était fausse d'un facteur trois — parce qu'une fenêtre de 60 s sur une base en WAL ne mesure rien de fiable, et parce qu'**un seul fichier n'est pas le disque**. La série du heartbeat existait depuis dix jours. **Chercher la série avant d'extrapoler l'instant.**
  - **`notebooklm.google.com` en dur retiré de `telegram_notify.py`** (seule occurrence du projet, ligne 151). Forme d'URL vérifiée sur le réseau avant de patcher : l'ancien host rend **301** vers `https://notebook.google.com/notebook/{id}` et le nouveau rend **302** vers son login — le chemin est reconnu. Lien contrôlé par rendu réel de `format_recap()`. md5 `27b4e980…` → `d6391509…` ; le `.bak-2026-08-23` reste l'original d'avant la session (`75fb45c9…`).

- **2026-08-23 (marche 1) — La porte du dédoublonnage teste désormais le VERDICT, pas l'observation.** Aucune des deux voies écrites la veille n'a été retenue telle quelle, parce que la reconnaissance en a invalidé les prémisses. **`is_seen()` et `filter_new_urls()` ne sont appelées par personne** — la vraie porte était le booléen de `mark_seen()` (`INSERT OR IGNORE`, `rowcount > 0`) à `main.py:118` ; « faire entrer `pushed_to_notebooklm` dans le filtre » aurait été un **no-op**. Et déplacer `mark_seen` après le push *supprime* une écriture : tout item recalé disparaîtrait de la base et serait re-scoré chaque semaine (~63 items de plus par run, indéfiniment), sans rollback possible. **Le manque commun aux deux options : la base n'avait aucun moyen de dire « celui-là a été jugé et recalé ».** Retenu : `is_resolved()` = `pushed = 1 OR kimi_score IS NOT NULL`, et chaque étape enregistre son verdict — `mark_resolved()` au filtre et au curateur, `mark_pushed()` au push. Ne subsiste « sans verdict » que la cohorte curée-non-poussée, celle qu'on voulait sauver. **Critère de sûreté d'Hector, pas d'élégance : cette voie n'ajoute que des écritures et ne fait que rétrécir une lecture** — la base garde strictement plus d'information qu'avant, le rollback tient en une ligne, et le coût Kimi est inchangé en régime permanent.
  - **La fuite était trois fois plus large que le diagnostic de la veille, et le push n'en était qu'un trente-septième.** Recompté sur les 12 logs de run : **112** items ont passé le seuil ≥7, **75** sont arrivés dans le notebook (= exactement les 75 lignes `pushed=1`). **37 items notés ≥7/10 n'ont jamais atterri** : 1 par échec de push (run du **08/06**, `push_failed: 1` — le défaut avait donc **déjà tiré**), 5 au run de mise au point du 03/06, et **31 écartés par le curateur**, dont **8 d'un coup le 15/06** (`filt=8, cur=0`). Le STEP 5 fuyait plus que le STEP 6.
  - **Deuxième panne silencieuse trouvée en chemin, et elle piégeait la correction : `filter_kimi.py` écrit `kimi_score = 0.0` quand le batch Kimi *échoue*.** Un 0 par panne réseau était indiscernable d'un 0 par jugement — enregistrer ce 0 comme verdict aurait enterré une cohorte entière, c'est-à-dire reproduit le bug en le déplaçant d'un étage. D'où le drapeau `kimi_ok`, et la même prudence côté curateur : **0 retenu sur N filtrés est traité comme une panne, pas comme un verdict** (cas du 15/06), rien n'est écrit et le lot repasse.
  - **Les 383 lignes héritées repassent au premier run — arbitrage d'Hector, option (a).** Elles portent toutes `kimi_score NULL`, donc « sans verdict ». Coût : un run à ~94 items chez Kimi au lieu de 31, une seule fois, quelques centimes. Contrepartie : **c'est le seul chemin qui rend les 37 perdus** encore présents dans la fenêtre de scrape. Ça s'auto-guérit dès le run n°2. Écarté : le backfill sentinelle, qui n'aurait protégé que l'avenir.
  - **`unjudged_count` escaladé dans `errors` — et le récap Telegram apprend à crier.** Demande d'Hector : « une panne Kimi partielle qui sort en exit 0, c'est exactement la famille qu'on répare ». Découvert en le câblant : **`format_recap()` n'affichait *jamais* `run_stats["errors"]`** — un run en erreur envoyait un message identique à un run sain, depuis le 03/06. Bloc « 🚨 Anomalies » ajouté ; il sert aussi `push_failed` et `notebooklm_auth_expired`, muets jusqu'ici.
  - **Prouvé, pas affirmé** : 42 assertions vertes en rejouant le **vrai `run_weekly_curation`** sur une **copie** de `state.db` (`state_db.STATE_DB` redirigé à chaud) — seuls le scrape, le scoring, la curation et l'appel CLI simulés ; porte, verdicts et boucle de push réels. Zéro réseau, zéro Kimi, zéro Apify, zéro Telegram, zéro NotebookLM. **`state.db` de production md5 `df5697de…` identique avant et après**, logs de prod non pollués. Sauvegardes `.bak-2026-08-23` sur les 5 scripts **et** sur `state.db` ; md5 encadrants : `state_db` `511be73f…`→`0da08968…`, `main` `beeac76a…`→`e13475f8…`, `filter_kimi` `2ab2e3ae…`→`1790047f…`, `notebooklm_pusher` `405f9240…`→`f48ff9b6…`, `telegram_notify` `75fb45c9…`→`27b4e980…`. `SYSTEM_PROMPT` du filtre vérifié byte-identique — le profil du 19/08 n'a pas bougé.
  - **Ce que le correctif ne fait pas, écrit avant qu'on l'oublie** : la récupération passe par le re-scrape, donc un item perdu *et* sorti de la fenêtre ne revient jamais. C'est la première next action du pivot.

- **2026-08-19 — Voix ajoutée, le trou le plus coûteux comblé.** Suivi direct des releases de `huggingface/speech-to-speech` (nouveau mécanisme `github.repos` + `scrape_github_releases()` dans `scrapers.py` — n'existait pas ; dédoublonnage par URL de release, donc chaque release future arrive une fois) + 3 queries STT / TTS / voice agent. Motif d'Hector : le socle vocal a subi le retrait de ses modèles le 16/08 ; « deux mois d'avance aurait tout changé ».
- **2026-08-19 — Polymarket : une query qui crée le flux, avec clause de mort datée.** Le focus_topic seul scorait un flux qui n'en contenait jamais (0 source en 12 runs). Revue au 07/09 après 3 runs : 0 item → on retire query et focus_topics. « Un critère qui ne rencontre jamais rien est pire qu'absent — il donne l'illusion d'une couverture. »
- **2026-08-19 — Instagram désactivé** (bloc commenté dans le yaml, daté) : 8 comptes scrapés à chaque run, 0 source retenue en 12 runs. Où va le budget libéré : les ajouts GitHub sont **gratuits** (API directe) ; Twitter reste bloqué par le plan Apify **et** par du code commenté, pas par le budget ; le seul réinvestissement Apify utile serait des chaînes YouTube supplémentaires (voix ?) — non décidé.
- **2026-08-19 — Ads-tech** : query `"meta ads" OR "marketing api" in:readme stars:>20` (validée à blanc : 10 repos).
- **2026-08-19 — Les trous assumés sont écrits dans le yaml** pour ne pas être redécouverts comme des oublis dans trois mois : jobboards (agent-alternance a `lba-fetch` sur ce VPS), ingestion média (media-vault a sa chaîne locale sur le Mac), stack produit Expo/Supabase/RevenueCat (hors périmètre par conception — cette veille couvre l'outillage d'agents, pas la stack des produits).
- **2026-08-19 (soir) — Le `SYSTEM_PROMPT` de `filter_kimi.py` mis à jour le jour même, sans attendre les 3 runs.** Arbitrage d'Hector : « Attendre 3 runs pour confirmer ce qu'on sait déjà, c'est trois semaines de veille mal filtrée avant lundi. » Le prompt datait du 03/06, jamais retouché, et décrivait un Hector de juin (« studio IA/no-code », stack Trigger.dev/FastAPI, trois projets — ni jarvis-voice ni ads-studio). Seul le bloc « Profil de Hector » change, reconstruit depuis les pivots du vault, avant/après validé en session ; mission, barème 0-10 et seuil 7 inchangés au mot près. Focus alignés sur les 13 focus_topics v1.1 ; agent-alternance laissé hors du profil (trou assumé jobboards) ; Polymarket maintenu jusqu'à la clause du 07/09. Sauvegarde `scripts/filter_kimi.py.bak-2026-08-19` ; md5 `ad7c6cb9…` → `2ab2e3ae…` ; `py_compile` + test d'import passés (venv réel, user veillebot, import seul — sans effet de bord).
  - **La ligne « stack produit HORS périmètre » est le seul ajout négatif du prompt, et le plus important** : « un profil qui ne dit que ce qu'il veut laisse passer tout ce qui y ressemble » (Hector). C'est elle qui empêche le tuto Supabase/Expo de passer le seuil en ressemblant au reste.
  - **Deux mois et demi sans relecture, pour le texte qui décide de TOUT ce qui entre dans la veille** : d'où la next action datée du 2026-11-01 — revue du profil, ou avant si un nouveau chantier majeur démarre.

## Ressources liées

- [[_hermes-polymarket]] — le venv partagé `notebooklm-veillebot/venv` et la culture d'audit du VPS
- [[_jarvis-voice]] — premier bénéficiaire des ajouts voix du 19/08
- [[2026-08-19-session]] — la reconnaissance NotebookLM (P4) qui a mené à ce pivot : connecteurs claude.ai morts, CLI non officiel vivant côté VPS, inventaire des 79 sources

## Journal du projet

### 2026-08-19
- Pivot créé (le projet tournait depuis le 03/06 sans note dans le vault).
- `sources.yaml` passé en v1.1 sur le VPS, sauvegardes datées (`sources.yaml.bak-2026-08-19`, `scripts/scrapers.py.bak-2026-08-19`) : voix (releases + 3 queries), Polymarket (query + clause de revue au 07/09), Instagram désactivé (0/12 runs), ads-tech, focus_topics alignés, trous assumés commentés.
- `scrapers.py` : ajout de `scrape_github_releases()` (releases, tags en pis-aller) — seul changement de code, compilé et testé à blanc via l'API GitHub réelle.
- Le soir, sur GO après avant/après : bloc « Profil de Hector » du `SYSTEM_PROMPT` de `filter_kimi.py` réécrit depuis les pivots (sauvegarde `filter_kimi.py.bak-2026-08-19`, md5 `ad7c6cb9…` → `2ab2e3ae…`, `py_compile` + import OK) ; le reste du prompt inchangé.
- Rapport automatique du premier run posé sur le Mac (demande d'Hector : « dis-moi lundi soir si la voix score ») : one-shot launchd `com.hectorvolant.secondbrain.veille-rapport-2408` — lun 24/08 20 h 04, rattrapage mar 25/08 9 h 33 — collecte VPS en lecture seule, rapport ajouté à `AI/logs/2026-08-24-session.md` + notification macOS avec le verdict, puis auto-désinstallation. Testé à blanc le 19/08.

### 2026-08-23
- **Panne découverte de biais.** La session partait d'une question sans rapport (inventaire des outils vidéo open source de la veille). L'interrogation du notebook a buté sur « Authentication expired » ; le diagnostic a remonté jusqu'au renommage de NotebookLM en « Gemini Notebook » et à la migration sur `notebook.google.com` (301 permanent mesuré sur l'ancien host).
- **Le VPS était déjà en panne, pas seulement exposé.** Diagnostic en lecture seule stricte (user `veillebot`, GO d'Hector) : CLI en 0.6.0, 27 occurrences de l'ancien host dans le paquet installé et 0 du nouveau, `Token fetch ✗ fail`. Intégrité vérifiée dans les deux sens — `storage_state.json` md5 `b91d9b4c…` et mtime `2026-08-19 09:33:51` **identiques avant et après** les commandes. Rien écrit sur le serveur pendant le diagnostic.
- **Point 1 tranché avant tout le reste, à la demande d'Hector** (« c'est lui qui décide si demain est risqué ou juste décalé ») : le « vu » est écrit au scrape. Demain était donc **risqué**, pas décalé. C'est ce résultat qui a déclenché la coupure.
- **Timer `veillebot-weekly` stoppé et désactivé** (GO d'Hector, seule écriture de la session sur le VPS). `mask` refusé par systemd, fichier d'unité intact. Vérifications complètes en Décisions.
- **Défaut de conception consigné en tête des next actions** : `mark_seen` au STEP 3 contre le push au STEP 6, sans rollback ni consultation de `pushed_to_notebooklm` dans le filtre.
- Chemin de retour documenté, avec l'avertissement `Persistent=true` : **réactiver, c'est déclencher un run dans la minute.**
