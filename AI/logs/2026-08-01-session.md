---
type: log
tags: [ia, automation]
cree: 2026-08-01
---

# Session du 2026-08-01

> **Continuité** : cette session prolonge sans interruption celle du 31/07 (13 sessions), la date ayant basculé en cours de travail. Nouveau fichier plutôt qu'une 14ᵉ section « même jour » — la convention `AI/logs/YYYY-MM-DD-session.md` prime, et **R7** vient justement d'acter qu'un log est une trace datée.

Mission : rafraîchir les 2 statuts que le rêve 2 signalait, inscrire deux leçons dans la constitution, et créer le pointeur global côté Claude.

## Modifiées

### Les 2 statuts périmés

- **`_agent-alternance`** — nouvel instantané **« Au 2026-07-31 — fin de journée »** empilé au-dessus de celui du même jour (conservé), qui était **faux à 0 jour d'âge**. Trois rectifications : le **5ᵉ doublon est archivé** (l'ancien le disait « laissé en `analysee` ») donc **5 au total et non 4** · le dépôt compte **16 fichiers suivis, pas 15** · **`scoring.json` est extrait**, config générique versionnée surchargée par `profil.json > scoring` resté hors dépôt. Ajout aussi de la dédup secondaire déployée. Le reste de l'instantané précédent tient et est explicitement confirmé.
- **`_second-brain`** — **premier instantané daté** de ce pivot. Le précédent avait **28 jours** et décrivait un vault qui n'existe plus : le pivot du vault était devenu la note la moins à jour du vault, relevé par les deux rêves. Le nouveau couvre le déménagement en `~/second-brain`, `/dream` v1.5 et ses six sections, l'automatisation launchd du dimanche, l'état réel des phases (1-2-6 faites, Phase 5 fermée par §6, Phase 4 et l'extraction des runbooks v4.0 ouvertes — cette dernière **orpheline** depuis que sa porte a été fermée sans reparentage). L'ancien texte est conservé au-dessous.

### Les 3 entrées de Journal manquantes

Le rêve 2 relevait que 3 sessions du 31/07 n'avaient pas d'entrée dans le Journal d'`_agent-alternance`. Ajoutées : dédup secondaire déployée · repo sous git + backup GitHub privé · `scoring.json` extrait. Le Journal de `_second-brain` reçoit de même les 4 lignes du 31/07 qui lui manquaient.

### La constitution — deux leçons inscrites (R6)

- **R7 ajoutée en §3** : « **Un log ne se réécrit jamais.** Les notes de `AI/logs/` sont des traces datées : on n'y corrige pas rétroactivement un chemin, un nom ou un chiffre, même devenu faux depuis. Ce qui a changé s'écrit dans la session suivante. » Avec sa formule de rattachement : *R1 protège les notes, R2 protège les mots d'Hector, R7 protège l'histoire.* Née du refus, la veille, de réécrire les 2 occurrences de l'ancien chemin dans les logs.
- **Leçon sur le moment d'écrire un statut, en §4** : « **Un `## Statut` s'écrit EN FIN de session, jamais au milieu.** La session qui rédige un statut est très souvent celle qui, dans l'heure qui suit, change les faits qu'il décrit. » Avec le cas qui l'a produite — l'instantané d'`_agent-alternance` faux à 0 jour.

## Créées

- **`~/.claude/CLAUDE.md`** (hors vault) — 3 lignes, miroir de `~/.codex/AGENTS.md` : le cerveau vit dans `~/second-brain/`, y lire `AGENTS.md` puis `AI/contexte-hector.md` pour tout contexte, **ne jamais y écrire depuis un autre projet sans demande explicite**. Le fichier n'existait pas — constat fait la veille pendant le déménagement, quand il figurait dans une liste de fichiers à corriger.
- Ce log.

## Décisions en attente de GO

1. **Deux blocs portent la même date dans `_agent-alternance`** (« Au 2026-07-31 — fin de journée » et « Au 2026-07-31 »). C'est le prix de la conservation demandée ; le premier dit explicitement qu'il rectifie le second. À fusionner si Hector préfère un instantané unique par jour.
2. **Boucles toujours sans réponse**, inchangées depuis le premier rêve : arbitrage Atlas (15 j) · `/grille-moi` jamais utilisé (22 j), ce qui gèle son portage en skill Codex · Phase 4 et `03-Resources/wiki/` toujours vide (22 j) · extraction des runbooks du doc v4.0, désormais orpheline.
3. **Côté alternance** : automatiser ou non le scoring (posé 7 fois le 31/07, jamais tranché) · la requête `automatisation` (2 offres en 19 jours) · les 7 fiches `à vérifier` de [[01-Projects/agent-alternance/cibles-directes|cibles-directes]] · **le contrôle du run VPS de 05 h 15 ce matin**, premier depuis le déploiement de la dédup secondaire.
4. **Obsidian** pointe peut-être encore sur `~/Documents/second-brain` si l'app n'a pas été relancée depuis hier.
5. **Premier créneau automatique réel du rêve : dimanche 20 h.**

## Notes de session

- Aucun `ssh`, aucune écriture VPS.
- Aucune modification des logs existants — R7 appliquée à elle-même dès sa première session.
- Commit de cette session : « constitution: R7 + leçon statut · statuts rafraîchis · pointeur Claude global » + push.

---

## Session 2 (même jour) — contrôle du run de 05:15 UTC

Premier run du scrape alternance **postérieur au déploiement de la dédup secondaire** (déployée le 31/07 à 10:19 UTC, soit après le run de 05:15 du 31/07 — d'où le report du contrôle).

### Lu (VPS, lecture seule stricte)

`systemctl --user status alternance-scrape.service`, `journalctl --user -u alternance-scrape.service --since "-1 day"`, `offres.json` et `offres.csv` via `common.load_offres()`, md5 de `scripts/common.py`, `list-units --failed`. **Aucune écriture.**

### Verdict : non-régression confirmée — mais la dédup reste non prouvée

**Vert sur tous les critères posés :**

| Critère | Résultat |
|---|---|
| `status=0/SUCCESS` | ✅ run du 01/08 05:15:09 UTC |
| `[merge]` cohérent, T = 59 + N | ✅ N = 0, T = 59 — voir la nuance ci-dessous |
| Collisions de clé toujours à 5 | ✅ 5, et **chacune n'a qu'une seule offre non archivée** |
| État vs baseline | ✅ identique : 59 offres · `analysee` 50 · `archivee` 5 · `envoyee` 3 · `refus` 1 |
| CSV | ✅ 59 lignes |
| `common.py` intact | ✅ md5 `1cc01a58…`, la version déployée |

**La nuance, et elle compte : le chemin de code de la dédup n'a toujours pas été exercé.** Les trois requêtes n'ont ramené qu'**une** offre (`intelligence artificielle` et `automatisation` en HTTP 204, `développeur` en 200 · 1 résultat), et cette unique offre a été **écartée par le filtre** (« 1 sans entreprise »). Le script a donc affiché « **Aucune offre collectée.** » et **n'a pas appelé `merge_offres`** — d'où **l'absence de ligne `[merge]`**, cohérente et non alarmante. `offres.json` n'a pas été réécrit (mtime toujours au 31/07 10:05, l'écriture du scoring).

Autrement dit : le run prouve que **rien n'est cassé**, il ne prouve pas encore que **la dédup fonctionne en production**. Il faudra un run qui collecte au moins une offre — et, pour voir une ligne `[dedup2]`, un run où France Travail republie une annonce déjà en base.

*(Les 3 unités `run-uXXXX` en échec sont les tests de sandbox ads-studio du 30/07, déjà documentés — sans rapport, pas une nouvelle régression.)*

### Modifiées

- `01-Projects/agent-alternance/_agent-alternance.md` — entrée de Journal du 01/08 avec le verdict **et sa réserve**.
- Ce log.

### Décisions en attente de GO

- **Rollback non déclenché** : aucune régression. `scripts/common.py.bak-20260731` reste disponible (8 197 octets, intact).
- **Le contrôle de la dédup reste ouvert** — à refaire au premier run qui collecte réellement une offre. Critère inchangé : collisions de clé toujours à 5, et jamais deux non-archivées dans un même groupe.
- Inchangées : arbitrage Atlas · `/grille-moi` jamais utilisé · Phase 4 et `wiki/` vide · scoring alternance à automatiser ou non · les 7 fiches `à vérifier` de [[01-Projects/agent-alternance/cibles-directes|cibles-directes]].
- **Premier créneau automatique du rêve : demain dimanche 20 h.**

### Notes de session

- Lecture seule intégrale sur le VPS, aucune écriture, aucun `.env` lu.
- Commit de cette session : « alternance: contrôle du run post-dédup (non-régression) » + push.
