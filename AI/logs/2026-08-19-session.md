---
type: log
tags: []
cree: 2026-08-19
---

# Session du 2026-08-19 — reconnaissance NotebookLM (P4)

Mission : reconnaissance, zéro code, zéro action. Brief initial (connecteurs, API, contournements), réorienté en cours de route par Hector : STOP recherche web, inventaire de l'existant, test réel sur « Veille IA Hector — Q3 2026 », contenu + utilité par projet. Rapport rendu en session. Lecture seule partout — Mac, vault, VPS, NotebookLM.

## Lues

- Vault : `AGENTS.md`, [[contexte-hector]], [[_jarvis-voice]], fiche-technique et decisions-et-lecons de hermes-polymarket, [[inventaire-projets]], [[2026-07-12-session]], [[2026-08-18-session]] (format de log).
- Mac, lecture seule : `~/.claude/skills/notebooklm/SKILL.md` (doc de teng-lin/notebooklm-py v0.3.4), `~/.claude.json` (mcpServers), `.claude/settings.local.json`, `~/.ssh/config` + historique zsh, `~/.notebooklm/` (profil du 26/04), `~/media-vault` (README, script inbox), LaunchAgents.
- VPS `root@openclaw`, lecture seule : unités systemd user de `veillebot`, `~/veillebot/` (`sources.yaml`, `scripts/`, `logs/2026-08-17.json`, `state.db`), skill Hermes `weekly_curation`, `~/.notebooklm/` de veillebot (`storage_state.json` du 17/08).
- NotebookLM via le CLI du VPS, lecture seule : `list`, `source list`, `artifact list` sur `9d6812e4` (Veille Q3 : 79 sources, 0 artefact) et `58a0d0eb` (« Claude Code » : 297 sources, 3 slide decks).
- Sondes : les deux connecteurs claude.ai « notebooklm » (`authenticate` → refus, « run /mcp ») ; `notebooklm auth check --test` sur le Mac (`token_fetch: false`).

## Modifiées

- (rien)

## Créées

- [[2026-08-19-session]] (ce log). **Non commité** — Hector lit le rapport d'abord.

## Faits saillants (le détail est dans le rapport de session)

- La connexion NotebookLM qui marche est le CLI **non officiel** `notebooklm-py` côté VPS (v0.6.0, venv `notebooklm-veillebot`) : auth vivante, cookies retournés par le run du lundi. Le CLI du Mac (v0.3.4) a une auth morte depuis ~fin avril. Compte Google des notebooks : `ndrcn937@gmail.com`.
- Les deux connecteurs claude.ai « notebooklm » sont des entrées mortes (un site web n'est pas un serveur MCP ; docs Anthropic vérifiées) ; « Notebooklm » pointait de surcroît sur le notebook « Claude Code », pas sur la veille.
- Dernier run `veillebot-weekly` : lundi 2026-08-17, 7 h 00 Paris — 94 scrapés → 31 nouveaux → 8 filtrés → 5 poussés, 0 erreur, 7 min 33.
- La liste amont des sources existe hors NotebookLM : `sources.yaml` + `state.db` + `logs/*.json` (titres, URLs, scores Kimi, tags de chaque item poussé).
- Recherche web interrompue sur ordre d'Hector (agent d'appui stoppé). Acquis avant l'arrêt : pas d'API grand public (API Enterprise seule, produit renommé « Gemini Notebook Enterprise » ; API consumer « in the works ») ; pas de connecteur NotebookLM officiel dans l'annuaire claude.ai.

## Décisions en attente de GO

- Supprimer (ou laisser) les deux connecteurs claude.ai « notebooklm » — ils ne peuvent pas fonctionner, quelle que soit l'authentification tentée.
- `notebooklm login` sur le Mac si un accès local est voulu ; sinon tout passe par le VPS, qui s'auto-entretient tant que le lundi tourne.
- Le vault ne contient aucune note sur la chaîne de veille (pivot ou runbook à proposer si Hector le veut).
- Commit de ce log — laissé à l'arbitrage de fin de lecture.

---

# Session du 2026-08-19 (suite — inventaire intégral du notebook Veille Q3)

*Ajoutée en fin de fichier, sans rien retoucher au-dessus : R7.*

Second livrable demandé par Hector : les 79 sources de « Veille IA Hector — Q3 2026 (Pro) » seules (titre, type, URL), familles déduites, rattachement aux projets, et les trous de la veille. Le notebook « Claude Code » est explicitement hors périmètre de cette réponse.

## Lues (en plus)

- Le JSON `source list` déjà capturé, relu en entier : les champs `type`, `url`, `created_at` y étaient.
- VPS, lecture seule : les 12 logs de runs `~/veillebot/logs/*.json` (2026-06-04 → 2026-08-17) et `sources.yaml` **complet** (sections twitter, github.queries, focus_topics, curation, budget Apify 5 $/run).

## Faits établis

- 79 sources = **75 poussées par la pipeline** (les dates d'ajout collent aux 12 lundis, 74 appariées URL à URL + agentclash apparié par sa date de run) + **4 manuelles** : Hermes Agent Documentation et une page docs.anthropic.com capturée en 404 (03/06, jour d'amorçage), deux « Texte collé » (09/06).
- **0 source Instagram retenue en 12 runs** malgré 8 comptes scrapés à chaque run ; Twitter présent dans le yaml mais désactivé (plan Apify).
- `focus_topics` contient « polymarket » et « prediction market », mais **aucune query ni chaîne ne porte le sujet** : le filtre score un flux qui n'en contient jamais.

## Décisions en attente de GO (en plus)

- Les trous relevés (voix/STT-TTS, Polymarket, ads-tech, jobboards, ingestion média, stack produit mobile) sont des candidats à `sources.yaml` — proposés dans le rapport, rien modifié.

---

# Session du 2026-08-19 (suite 2 — GO reçu : corrections sources.yaml + pivot)

*Ajoutée en fin de fichier, sans rien retoucher au-dessus : R7.*

GO d'Hector sur les trous : corriger `sources.yaml` en une session avec sauvegarde datée, puis créer le pivot [[_veille-ia]]. Exécuté et validé.

## Lues (en plus)

- VPS, avant toute écriture : `scrapers.py`, `state_db.py`, `filter_kimi.py`, `main.py`, `config.py` en entier — pour modifier selon ce que le code consomme réellement. Trois faits qui ont dicté l'implémentation : dédoublonnage par hash d'URL (une release = un item neuf, une seule fois) ; `scrape_all()` saute Instagram si la clé est absente (désactivation sûre) ; les `focus_topics` nourrissent le prompt du filtre Kimi (sans topics voix/ads, les nouvelles queries seraient massacrées au scoring — le piège Polymarket, inversé).

## Modifiées (VPS — hors vault, sur GO)

- `~/veillebot/sources.yaml` → **v1.1** (sauvegarde `sources.yaml.bak-2026-08-19`) : voix (3 queries + section `github.repos` avec `huggingface/speech-to-speech`), query Polymarket avec clause de revue au 07/09 écrite dans le fichier, Instagram commenté et daté (0 retenu en 12 runs), query ads-tech, 4 focus_topics alignés, trous assumés consignés en commentaire de tête (alternance → lba-fetch, media-vault → chaîne locale, stack produit → hors périmètre par conception).
- `~/veillebot/scripts/scrapers.py` (sauvegarde `scrapers.py.bak-2026-08-19`) : ajout de `scrape_github_releases()` — le suivi de releases n'existait pas ; releases, tags en pis-aller, branché dans `scrape_all()`. Seul changement de code.
- Garde-fou avant application : comparaison structure à structure du yaml neuf contre le fichier vivant (zéro dérive sur les sections intouchées), chaînée en `set -e` — rien ne s'appliquait si elle échouait.
- Validation à blanc après application : compilation OK, les 5 nouvelles queries rendent 10 dépôts chacune contre la vraie API GitHub, releases speech-to-speech captées (v0.2.10 → v0.2.12). Zéro Apify, zéro Kimi, zéro écriture en base, zéro push.

## Créées

- [[_veille-ia]] (`01-Projects/veille-ia/`) — pivot : chaîne complète, source de vérité hors NotebookLM (`sources.yaml` + `state.db` + `logs/*.json`), les cinq décisions datées du 19/08, next actions (récap du 24/08, surveillance du scoring voix, revue Polymarket au 07/09).

## Décisions en attente de GO

- Le `SYSTEM_PROMPT` de `filter_kimi.py` décrit un Hector sans jarvis-voice ni ads-studio — à mettre à jour **si** les items voix se font massacrer au scoring des 3 prochains runs (next action du pivot, décision non prise ce jour).
- Restent ouvertes de la reconnaissance du matin : suppression des deux connecteurs claude.ai morts, `notebooklm login` sur le Mac.
