---
type: log
tags: [ia, dev, automation]
cree: 2026-08-03
---

# Session du 2026-08-03

> **Continuité** : cette session prolonge celle du 02/08, la date ayant basculé en cours de travail (22 h UTC = 00 h Paris). Nouveau fichier plutôt qu'une section « même jour » — la convention `AI/logs/YYYY-MM-DD-session.md` prime, et **R7** acte qu'un log est une trace datée.

Mission : combler les deux trous relevés par le rêve du 02/08 (DREAM CONSOLE absent du vault, carte du VPS en retard d'un service), puis diagnostic de stockage du VPS en lecture seule.

## Lues

- `AI/runbooks/documenter-un-projet.md` **en entier** — la procédure a été suivie section par section.
- `Templates/tpl-project.md`, `01-Projects/legitvision/fiche-technique.md` (référence de format).
- `AI/dreams/2026-08-02-dream.md` — les deux incohérences à traiter.
- `~/dream-viewer` (hors vault) : `package.json`, arborescence, `git log`, imports réels — **aucune modification du dépôt pendant l'import** (runbook §2).
- **VPS en lecture seule stricte** : `df`, `du`, `find`, `stat`, `lsof`, `journalctl --disk-usage`, et `sqlite3 -readonly` en requêtes **bornées sur index** (`MAX(rowid)`, `MIN/MAX` sur colonne indexée, `PRAGMA page_count`). **Aucun scan de table, aucune écriture, aucun `.env` lu.**

## Créées

- **`01-Projects/dream-console/_dream-console.md`** — note pivot (`statut: actif`, `tags: [ia, dev, automation]`), premier instantané daté du 02/08.
- **`01-Projects/dream-console/fiche-technique.md`** — stack réelle, architecture, six modules, état de prod, pointeurs.
- **`01-Projects/dream-console/decisions-et-lecons.md`** — dont « **une propriété de sécurité non testée n'existe pas** ».
- Ce log.

## Modifiées

- **`01-Projects/hermes-polymarket/fiche-technique.md`** — ajouts seuls (R2) : le compte de timers passe de 14 à **20**, `hermes-collector` apparaît en unité **système**, et une section « Sonde externe » consigne les deux constats de lecture seule (gel de `state.db` depuis le 13/07, divergence de finalisation du 14/07).

## Correction d'une consigne, sur preuve

La consigne d'import mentionnait « **Next.js + Three.js** ». Le projet **n'utilise pas Three.js** : 0 import, absent du `package.json`. Le rendu graphique est **SVG + Canvas 2D**. La fiche technique dit ce qui est, pas ce qui était supposé. Au passage, `gray-matter` — prévu au départ — était installé sans jamais être importé : retiré du projet le 02/08.

## Diagnostic de stockage du VPS — rapport, aucune action

**Disque : 75 Go, 46 Go utilisés, 27 Go libres (64 %).**

Croissance mesurée sur **trois points** espacés (02/08 08:17, 21:45 et 22:07 UTC) :

| poste | taille | croissance mesurée |
|---|---|---|
| `bot3.db` | 1,73 Go | **~700 à 920 Mo/jour** (moyenne depuis l'origine : 494 Mo/j) |
| `polymarket.db` | 5,88 Go | **~90 Mo/jour** (deux fenêtres concordantes : 93 et 89) |
| journald | 4,0 Go | sans `SystemMaxUse` : plafond implicite ~7,5 Go, donc ~3,5 Go de marge |
| `/var/lib/docker` | **16 Go** | statique |
| `/var/lib/containerd` | **6,7 Go** | statique |

- **Croissance active combinée : ~0,8 à 1,0 Go/jour**, soit **24 à 30 Go/mois**.
- **Saturation estimée entre le 29 août et le 5 septembre 2026** (27 à 34 jours).
- `bot3.db` collecte depuis le **30/07 10:01** : 1,1 M de lignes de `trades` en 3,5 jours. Le backfill est **à jour** (`pending=0`, âge des cycles ~0,1 h) : la croissance observée est un régime permanent, pas un rattrapage qui s'arrêterait.
- Aucune des deux bases n'a de `freelist` : un `VACUUM` ne rendrait rien.
- **Faux positif écarté** : un fichier de 5 Go apparents dans `/tmp` (`bot3_v2_…db.gz`) est **creux** — occupation réelle **0 octet**. Il n'y a pas de gain de 5 Go à récupérer là.

**Le plus gros bloc récupérable est Docker + containerd, 22,7 Go — mais il est statique et son sort est une décision, pas un nettoyage.** Aucune action entreprise.

## Décisions en attente de GO

1. **Saturation disque sous ~30 jours** : arbitrer entre rétention/rotation sur `bot3.db`, purge Docker/containerd, ou extension du volume. Rien n'a été touché.
2. **`~/dream-viewer` n'a aucun remote git** : c'est le seul projet actif sans backup hors du Mac.
3. **L'exception `ReadWritePaths`** du collecteur (mesurée, documentée, réversible en une ligne) reste à confirmer ou à refermer.
4. Le rêve du 02/08 attend toujours son arbitrage : 14 propositions sans réponse, 3 statuts périmés, 8 boucles ouvertes, 2 graines de wiki.

## Notes de session

- Aucune modification d'Hermes, aucun redémarrage de service, aucune écriture sur le VPS hors `~/hermes-collector/` et l'unité du collecteur.
- Le rêve du 02/08 et son log sont commités dans cette session, sur demande explicite d'Hector — le §4 de `/dream` les laisse volontairement non suivis, c'est Hector qui lève cette réserve.

---

## Session 2 (même jour) — arbitrages stockage et filets

Mission : mettre DREAM CONSOLE sous filet, plafonner journald, auditer Docker, lire la spec de Bot 3, refermer l'exception du collecteur, ajouter une jauge disque au cockpit.

### Créé / modifié hors vault

- **Dépôt privé `lixyra05-debug/dream-viewer`** — poussé après scan : ni IP, ni hôte, ni URL de tunnel, ni secret, ni chemin absolu du Mac. **Deux éléments de contenu du vault retirés du code** : une phrase du rêve recopiée en dur dans le module MIROIR, et les chiffres du verdict Atlas en repli du réticule.
- **`/etc/systemd/journald.conf.d/size.conf`** — `SystemMaxUse=1G` (**écriture système validée par Hector**). Journal passé de **4,0 Go à 956 Mo**, `systemd-journald` seul redémarré. Le plafond implicite de 10 % du disque (~7,5 Go) est supprimé.
- **Collecteur** — l'exception `ReadWritePaths` est **supprimée**, pas déplacée : elle pointait sur `~/.hermes/skills/copy_trader/data`, hors du périmètre accordé. Le collecteur recopie `atlas.db` dans son `/tmp` privé (`PrivateTmp`) et lit la copie. Vérifié : `ReadWritePaths=` vide, `ProtectHome=read-only`, `IPAddressDeny` actifs.
- **Jauge disque** dans le panneau HERMES, avec projection de saturation à 30 jours.

### Modifiées dans le vault

- `01-Projects/dream-console/_dream-console.md` — mise sous filet, exception refermée, journal du 03/08.

### Audit Docker — aucune action (STOP demandé)

`/var/lib/docker` **16 Go** + `/var/lib/containerd` **6,7 Go**.

| poste | taille | statut |
|---|---|---|
| volume `trigger_clickhouse` | **7,9 Go** | ACTIF — 73 628 fichiers touchés en 7 j |
| volume `trigger_postgres` | 2,4 Go | ACTIF |
| `containerd/overlayfs` | 5,3 Go | snapshots |
| `docker/rootfs` | 4,3 Go | couches |
| `docker/containers` | 865 Mo | **9 970 dossiers**, dont 18 Mo de logs |
| 3 volumes orphelins | **0 B** | rien à gagner |

- **9 961 conteneurs `exited`** (9 118 en juillet, 845 en août), le plus ancien du 10/07 — un `runner-*` Trigger.dev toutes les ~5 min, jamais nettoyé.
- **L'orphelin n'est pas un volume, c'est un conteneur en boucle** : `trigger-electric-1` (electricsql/electric:1.2.4), créé le **13/05**, **6 859 redémarrages**, crash Erlang à chaque cycle.
- Gain d'un `container prune` : **~850 Mo certains** (Docker n'annonce que 40,8 Mo car il ne compte que les couches inscriptibles, pas les 9 970 dossiers). Images et volumes : **0 B récupérable**, tous en usage.
- **À ne pas toucher** : les 8 conteneurs `Up 2 months (healthy)` de la pile Trigger.dev dont dépend [[_autonomous]].

### Spec de Bot 3 — ce que le backtest consomme

La spec le dit elle-même : **`rtds_ticks` et `book_snapshots` sont IRREMPLAÇABLES ; `trades` se re-télécharge depuis data-api.** L'horloge du backtest est `rtds_ticks.oracle_ts_ms` (jamais `ingest_ts_ms`), avec des critères à ±3 s des bornes de cycle : **ce sont des ticks, pas des barres 5 min** — mais le flux de ticks ne pèse que 0,026 Gio/j.

Croissance mesurée par le projet lui-même (dbstat) : **0,735 Gio/j** — `trades`+index **0,40** · `book_snapshots`+index **0,30** · `rtds_ticks` 0,026 · `hb` 0,008. Concordant avec ma propre mesure sur 13,5 h (0,7 à 0,9 Go/j).

**Le poste dominant est donc exactement celui qui est reconstructible.**

⚠️ **Anomalie signalée, non tranchée** : le bloc « VOLUMÉTRIE ET BACKUP » de `bot3_etape5_spec_2026-08-01.md` décrit `bot3.db` à **10,71 Gio** avec des archives datées des **13 et 14 août**, alors que le fichier réel fait **1,64 Go aujourd'hui**. Soit c'est un exemple travaillé écrit au futur, soit une partie de la chaîne se date elle-même en avance. Les chiffres de croissance, eux, sont corroborés par une mesure indépendante.

### Décisions en attente de GO

1. **Rétention `trades`** — la seule coupe qui vaille : −54 % de croissance, 0,735 → ~0,335 Gio/j. Doit passer par le processus prévu : les **16 triggers R5 refusent `UPDATE` et `DELETE`**, seul `INSERT OR IGNORE` passe. Un DELETE nu est impossible par construction, et c'est une bonne nouvelle.
2. **`trigger-electric-1` en boucle depuis 3 mois** — 6 859 redémarrages. Rien touché.
3. **`docker container prune`** — ~850 Mo, à ordonner.
4. **Volume Hetzner (~1 €/mois les 20 Go)** — retarde d'environ un mois au rythme actuel, ne résout rien.

### Notes de session

- Aucune modification d'Hermes, aucun redémarrage de service hors `systemd-journald` (autorisé) et `hermes-collector` (le nôtre).
- Aucune action sur Docker, containerd ou `bot3.db` : audits en lecture seule uniquement, comme demandé.

---

## Session 3 (même jour) — arbitrages exécutés

### Fait

- **Purge Docker** — `docker container prune` sur les conteneurs **arrêtés** uniquement : **10 150 → ~6 100** conteneurs, `containers/` de **865 Mo à 558 Mo**, disque de 62 % à **57 %**. Les 9 conteneurs actifs de la pile Trigger.dev n'ont jamais pu être concernés : `prune` ne touche ni `running` ni `restarting`, par construction.
- **Cause traitée à la source** — le superviseur Trigger.dev crée ses runners avec `AutoRemove=false` et **aucune variable de rétention n'existe** dans sa config (ni `.env`, ni environnement du webapp). D'où `docker-prune-runners.timer`, unité **système**, **lundi 04 h** (à l'écart des créneaux d'Hermes 05:00-05:45 et du rêve du dimanche 20 h), filtre `until=168h`, `Nice=15` + `IOSchedulingClass=idle`.
- **journald** plafonné à 1 Go (session 2) : 4,0 Go → 956 Mo.

### Diagnostic `trigger-electric-1` — rapport, aucune action

**Cause exacte** : `failed to connect to Postgres: (Postgrex.Error) FATAL 28P01 (invalid_password)`. C'est une **dérive de configuration**, pas un problème de ressource — l'alarme mémoire Erlang visible dans le journal est un leurre.

**Preuves qu'il n'est requis par personne, dans l'usage actuel :**
- `webapp.depends_on` = `postgres, redis, clickhouse` — **electric n'y figure pas**.
- **0 mention d'electric dans les journaux du webapp sur 24 h** : l'application ne se plaint jamais de son absence.
- **10 150 runners, tous `Exited (0)`, zéro échec.** Le pipeline d'exécution est intact.
- `trigger-webapp-1` est **healthy** depuis 2 mois.
- **7 447 redémarrages depuis le 13 mai** : electric n'a jamais fonctionné, pas une fois, depuis l'installation. [[_autonomous]] est passé en production et a tourné en juillet sans lui.

Ce qu'il porterait s'il marchait : l'API **Realtime** de Trigger.dev (`subscribeToRun`, rafraîchissement live du tableau de bord). Jamais disponible ici, donc jamais utilisée.

**Deux issues, à arbitrer** : l'arrêter (gain : une boucle de crash de ~53 redémarrages/heure qui cesse) ou **le réparer** — c'est un mot de passe, une ligne. Rien touché.

### Rétention `trades` — CONFLIT AVEC R5, non implémentée

Le GO ne peut pas être exécuté tel quel, et voici pourquoi, avec les pièces :

- **R5 est absolue dans ce projet** : **16 triggers** `r5_no_delete_*` / `r5_no_update_*` sur les **8 tables**, chacun un `RAISE(ABORT, 'append-only (R5) : DELETE interdit sur …')`. `report.py` l'écrit en tête : « `UPDATE` et `DELETE FROM` sont INTERDITS (R5), sur toutes les tables ».
- **Le « mécanisme d'archive sélective existant » ne coupe rien** : `bot3_v2sel_*` exclut `trades` et `cycle_outcomes` de **l'archive** (`TABLES_EXCLUES`), il ne retire aucune ligne de la base vivante. Le seul « purge » du code concerne les **fichiers .gz**, jamais des lignes.
- **Aucun processus de coupe n'existe** dans le dépôt. En construire un exige soit de désactiver les triggers — ce qui détruit la garantie que le projet s'est donnée — soit de **reconstruire le fichier** (nouvelle base, `INSERT` des lignes conservées, vérification, échange atomique). La seconde voie est viable et réutilise ~90 % du code de copie sélective déjà écrit, mais c'est un **changement structurel** sur l'actif principal du projet.
- **Et elle ne rendrait rien aujourd'hui** : `trades` couvre du **31/07 au 03/08**, soit **0 ligne au-delà de 7 jours (0,0 %)**. Une rétention à 7 jours ne coupe rien avant le **07/08**.

**Rien n'a été écrit, aucun timer posé.** L'argument de fond reste juste — une base qui grossit finit par interdire sa propre sauvegarde — mais il se traite par une reconstruction contrôlée, pas par une coupe, et ça se décide.

### Créée

- `00-Inbox/Spec Bot 3 — un bloc daté du 14 août dans un fichier du 1er.md`

### Décisions en attente de GO

1. **Rétention `trades`** : autoriser une **reconstruction-échange** (le seul chemin compatible R5), ou renoncer. Aucune urgence avant le 07/08.
2. **`trigger-electric-1`** : arrêter, ou réparer le mot de passe.
3. Le rêve du 02/08, toujours non arbitré.

### Notes de session

- Aucune modification d'Hermes. Services redémarrés : `systemd-journald` (autorisé) et `hermes-collector` (le nôtre). Aucun service Trigger.dev touché.
- Écritures système : `/etc/systemd/journald.conf.d/size.conf`, `/etc/systemd/system/docker-prune-runners.{service,timer}`, `/usr/local/bin/docker-prune-runners.sh`.

---

## Session 4 (même jour) — electric arrêté, procédure de coupe écrite et testée

### Fait

- **`trigger-electric-1` ARRÊTÉ** — `docker update --restart=no` puis `docker stop`. **Pas de `rm`** : le conteneur existe toujours, la réactivation est une ligne, documentée dans [[01-Projects/autonomous/fiche-technique|autonomous — fiche-technique]] avec l'avertissement qui va avec (le mot de passe doit être corrigé **avant** relance, sinon la boucle reprend à 53 redémarrages/heure). Les 8 services restants sont `healthy`, les runners continuent de sortir en `Exited (0)`.
- **Purge Docker terminée** : **10 150 → 42 conteneurs**, disque à **32 Go libres** (était 27 Go ce matin, 64 % → 56 %).

### `bot3_trim` — procédure écrite, testée, NON exécutée en production

`ops/bot3_trim.py` (versionné dans `~/dream-viewer`, déployé dans `~/bot3-trim/` — **le dépôt de Bot 3 n'a pas été touché**).

**Le principe, qui règle le conflit avec R5** : le script ne supprime jamais une ligne. Il **construit une base neuve par INSERT** des seules lignes conservées, la vérifie, puis remplace le fichier. Les 16 triggers sont recréés à l'identique et **leur efficacité est prouvée avant tout échange** — le script tente un `DELETE` sur la base neuve et exige qu'il soit refusé.

**Vérifications avant échange** : `quick_check` · `user_version` · liste des tables · les 16 triggers nommément · la liste des index · les comptes de lignes table par table, avec détection de perte sur les tables non coupées · la preuve R5. **Une seule qui tombe annule tout et laisse l'original intact.**

**Deux gardes d'exécution, comme demandé** :
1. rien à couper → sortie 0, sans action (cas nominal avant le 07/08) ;
2. sinon, il faut `disk_free < 20 Go` **ou** `--go` explicite.
Plus une garde d'espace (`taille + 5 Go` de pic requis) et un `flock`.

**Test sur une COPIE, rétention 1 j** — la machinerie complète, échange inclus :
```
trades      1 407 517 → 538 046 lignes (61,8 % coupés)
7 autres tables       copiées intégralement, comptes identiques
vérifications         toutes passent, dont la preuve R5
taille                2,00 Go → 1,31 Go (−35 %)
durée                 19,7 s de construction + 26 s de vérification ≈ 46 s
```
Ces 46 s sont l'ordre de grandeur de l'**interruption du collecteur** en production. Un défaut a été corrigé pendant le test : sans garde, `--db <copie>` arrêtait quand même les **vrais** services. Le script vérifie désormais que la cible est bien la base de production avant de toucher à un service.

**Test sur la VRAIE base, rétention 7 j** : `RIEN À COUPER — sortie sans action`, code 0. C'est le comportement attendu jusqu'au 07/08.

### Décisions en attente de GO

1. **Exécution de `bot3_trim` en production** — conditionnée : lignes > 7 j (pas avant le 07/08) ET (`disk_free < 20 Go` OU GO d'Hector). Rien n'est programmé : **aucun timer**, l'appel reste manuel par construction.
2. Le rêve du 02/08, toujours non arbitré.

### Notes de session

- Aucun `rm` de conteneur nommé, aucun service Trigger.dev arrêté hors `trigger-electric-1`. Le dépôt `~/bot3_btc5min` n'a reçu aucune écriture.
- 3,5 Go de fichiers de test supprimés après vérification.

---

## Session 5 (même jour) — arbitrage du rêve du 02/08

Reprise après coupure de limite hebdomadaire : les lots 2 à 5 étaient commités localement mais **non poussés**, l'arbre était propre des deux côtés.

### Lot 1 — déjà fermé par le tirage, acté

Deux propositions du rêve du 02/08 sont **appliquées**, sans rien à refaire : la création du pivot [[_dream-console]] et de ses deux notes (02-03/08), et la mise à jour de la carte du VPS avec `hermes-collector` dans [[01-Projects/hermes-polymarket/fiche-technique|hermes — fiche-technique]]. Le rêve les avait relevées comme des trous ; ils étaient comblés avant même son arbitrage.

### Lot 2 — corrections mécaniques

Capture Hermes classée dans son projet (l'Inbox ne garde que l'anomalie Spec Bot 3) · `_agent-alternance` passe à 16 fichiers (HEAD `d90b5d4`, vérifié sur le serveur) · `_second-brain` re-daté « Au 2026-08-01 (session débordée du 31/07) » — le log a raison, le pivot s'aligne (R7) · [[inventaire-projets]] : 3 lignes corrigées, chacune avec la mention de sa correction · [[contexte-hector]] : VPS sous `veillebot`, RevenueCat intégré le 10/07, **annotés et non effacés** (R2) · runbook [[dream]] : la notification d'échec conseille l'emplacement du vault, plus l'accès disque complet.

**Les 5 pivots « (à remplir) » sont remplis par transcription datée** des statuts et des logs — aucune invention. 17 décisions et 11 next actions entrent dans les pivots, là où elles ne vivaient que dans les logs et la conversation.

### Lot 3 — vérifications en lecture seule

- **`ads-digest` : verdict positif.** Le timer a tiré **dès son premier créneau attendu**, le 31/07 à 05:00 UTC (`digest envoyé (850 c.)`, exit 0), et tous les jours depuis. *J'avais d'abord lu une fin de journal pour un début et conclu à tort qu'il n'avait pas tiré avant le 01/08 ; corrigé sur relecture.* Aucun diagnostic de panne à faire.
- **Les 2 offres sans employeur** : recherche menée, **employeur non identifié** — les deux URL `fr.trabajo.org` répondent **410 Gone**. Passées en `a_eviter` par la voie sanctionnée (`flock .offres.lock` + `save_offres`, backup `.bak`), avec les pistes non confirmées consignées **comme non confirmées**.
- **Les 7 fiches « à vérifier »** : **5 levées** (Kairos → `sacha@sksolutions.io` tranché ; Calk AI → Quentin Fournier confirmé par sa propre publication ; Volteyr → Hany Mohsen président au registre, SIREN 992475210 ; Hyperstack → « Louis » = Louis Adam, seule agence française n8n Expert Partner ; DevFlows → Nadjib Mellak gérant). **2 non levées et marquées comme telles** (Massive Dynamic : rien à lever, c'était une mise en garde ; Dev Together : fondateur jamais nommé). Au passage, la fiche DevFlows était périmée — **SASU transformée en SARL le 21/02/2025**.
- **Twitter/X retesté** : `402 Payment Required` là où le 31/07 relevait un `404`. Le mur a changé de nature — accès payant, plus point d'entrée cassé. Canal toujours hors de portée.

### Lot 4 — décisions structurelles actées

- **Scoring alternance : question fermée après 8 occurrences.** Manuel **par design** — zéro token dans les crons, HITL absolu. **Contrepartie déployée** : le brief de 07 h 45 compte les offres restées en statut `nouvelle` et **rompt le silence** si le stock repart. Il vaut **0** aujourd'hui, donc aucun bruit ajouté — c'est une sentinelle, pas un bavardage.
- **Requête France Travail** : `automatisation` (2 offres en 19 jours) → `data`. Rendement à évaluer vers le **17/08**, next action posée.
- Les deux patchs déployés avec la discipline complète : md5 avant/après, `.bak` daté, `py_compile`, **test fonctionnel sans effet de bord** (import du module plutôt que `--force`, qui aurait envoyé un Telegram), commit et push du dépôt. **Vérification au run de 05:15 demain.**
- **AGENTS.md §2** : « `02-Areas/` s'amorce par l'usage humain — vide n'est pas un défaut structurel tant que la capture n'a pas démarré. »
- **Extraction des runbooks du doc v4.0 : ÉCARTÉE définitivement.** Le vault documente mieux que le doc — 4 runbooks écrits depuis le réel contre une archive de juin. **Boucle de 29 jours fermée par un non.**
- **Nouveau runbook** [[vps-git-et-deploiement]] — la séquence rejouée 4 fois, doctrine « lecture seule d'abord » en tête.

### Lot 5 — Phase 4 s'ouvre sur UNE page

[[La panne silencieuse]] — première page de `03-Resources/wiki/` après 23 jours de dossier vide. Cinq formes cataloguées, chacune sourcée sur une occurrence réelle et datée, chacune avec sa **signature** ; six parades déjà éprouvées ici. Bandeau « proposition Claude — à valider ». Backlog Phase 4 consolidé : **6 graines**, dont les 3 jamais versées.

### Lot 6 — Atlas : procédure préparée, RIEN EXÉCUTÉ

[[01-Projects/hermes-polymarket/mise-en-pause-atlas-procedure|mise-en-pause-atlas-procedure]]. Relevé en lecture seule des 4 unités, **3 granularités** proposées (tout arrêter / arrêter la copie en gardant l'observation / suspendre sans désarmer), commandes réversibles avec leur inverse, et la liste de ce qui est conservé — **c'est-à-dire tout**, aucune donnée n'est touchée par un `stop`.

Trois points relevés qui n'étaient pas dans la demande : `atlas-validation-review` est **déjà consommé** (`elapsed`, plus aucun tir) et sert aujourd'hui de rappel visible dans `list-timers` — le laisser a une valeur ; une pause **ne clôture pas les 10 positions ouvertes**, qui resteront ouvertes indéfiniment puisque le monitor ne tournera plus ; et **aucune clé privée n'existe sur le serveur**, donc une pause ne comporte aucun risque financier.

### Boucles restantes, assumées ouvertes

1. **Dédup secondaire** — critère écrit d'avance (`exit 0` + ligne `[dedup2]`), **attend un run qui collecte un doublon réel**. Déployée et testée, pas encore prouvée en production.
2. **`/grille-moi` jamais utilisé** — gate **humain**, inchangé ; le portage en skill Codex reste gelé derrière.
3. **`_autonomous` — clarification SIRET/SEPA** — décision d'Hector, en attente. Seule boucle du vault qui bloque un encaissement.
4. **`_vivo` — compte Apple Developer** — décision d'Hector, en attente.
5. **Arbitrage Atlas keep / adjust / stop** — la procédure est prête, l'arbitrage reste entier.
6. **Rétention `trades` de `bot3.db`** — `bot3_trim` écrite et testée sur copie, conditionnée : rien à couper avant le 07/08.
7. **Anomalie de datation de la spec Bot 3** — en Inbox, non tranchée, attend le contexte d'Hector.

### Notes de session

- Aucune écriture sur le VPS hors : les 2 patchs de `~/alternance/scripts/` (avec `.bak`, commités et poussés dans leur dépôt) et le classement des 2 offres via `flock` + `save_offres`. **Aucune commande Atlas exécutée.**
- Une erreur de lecture commise et corrigée dans la même session (le journal d'`ads-digest`), consignée ci-dessus plutôt que réécrite.
