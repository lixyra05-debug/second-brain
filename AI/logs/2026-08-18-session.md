---
type: log
tags: []
cree: 2026-08-18
---

# Session du 2026-08-18

## Lues
- [[AGENTS]]
- [[contexte-hector]]
- [[tpl-log-session]]

## Modifiées
- 

## Créées
- `Desktop/Skills higgsfields /Plateforme de lancement de campagne/skill.md` — skill Higgsfield fourni par Hector, conservé intégralement.
- [[2026-08-18-session]]

## Décisions en attente de GO
- 

---

# Session du 2026-08-18 (seconde session — arbitrages jarvis-voice)

*Ajoutée en fin de fichier, sans rien retoucher au-dessus : R7, un log ne se réécrit pas.*

Mission : exécuter les arbitrages d'Hector sur la session du 17/08 — **option 2** pour le
détecteur de frontière (« dater les empreintes contre l'historique git »), puis commit +
push des trois cibles : détecteur, migration LLM, vault. Écriture dans `~/jarvis-voice`
**et** dans le vault, sur demande explicite.

## Lues

- Vault : [[_jarvis-voice]] (diff non commité du 17/08), [[2026-08-17-session]],
  `AGENTS.md`.
- Hors vault : `results/MIGRATION-LLM.md`, diffs de `RESULTAT.md` et `talk_fr.sh`,
  `bench/fuite_vault.py`, `bench/scan_avant_commit.sh`.

## Modifiées

- Hors vault, dépôt `~/jarvis-voice` :
  - `bench/fuite_vault.py` — **option 2 implémentée.** Chaque reprise est datée contre
    les deux historiques (`git log` / `ls-tree` / `cat-file`, trois commandes de lecture).
    Verdicts : **fuite** (le vault l'avait d'abord — bloque), **dérivé** (le dépôt l'avait
    d'abord — passe, compté), **indéterminé** (commité d'aucun côté — bloque). Le
    dénominateur s'imprime avec chaque verdict : commits parcourus, durée.
  - `bench/scan_avant_commit.sh` — libellé final corrigé (« aucune fuite » au lieu de
    « 0 correspondance », devenu faux dès qu'un dérivé passe).
  - `results/MIGRATION-LLM.md` — une phrase reformulée : « tombées précisément là, jamais
    au banc » venait du pivot du 15/08. Première vraie traversée vault → dépôt, attrapée
    par le détecteur neuf à son premier essai réel — l'ancien, symétrique, n'aurait pas
    su la distinguer des 75 dérivés qui l'entouraient.
- Vault : [[_jarvis-voice]] — next action « Arbitrer le détecteur » cochée, décision du
  2026-08-18 ajoutée, entrée de journal.

## Éprouvé avant de servir *(panne n° 8 : un garde-fou qu'on n'a jamais vu mordre n'est pas validé)*

| essai | attendu | obtenu |
|---|---|---|
| fuite synthétique — six mots de [[contexte-hector]] (commité en juillet) | FUITE, exit 1 | ✓ daté « vault 2026-07-04 · dépôt non commité » |
| fichier sans matière du vault | 0 reprise, exit 0, datation non exécutée | ✓ |
| cas réel (`RESULTAT.md` + `MIGRATION-LLM.md`) | dérivés + indéterminés | ✓ — et 1 fuite réelle en prime, reformulée |

## Coût, chiffré comme demandé

0 s sur un scan propre (la datation ne tourne que s'il y a des reprises) ; 0,4 s pour
4 reprises ; **3,3 s pour 99 reprises contre 59 + 10 commits** — le pire cas réaliste,
celui d'une session de documentation. Pas de dérapage : le repli sur l'option 1 n'a pas
lieu d'être.

## Arbitrage manuel, consigné

Scan d'avant-commit des 14 fichiers : **0 fuite, 75 dérivés, 24 indéterminées**, motifs
secrets / chemins / hôtes muets. Les 24 indéterminées sont la matière du 17/08, non
commitée des deux côtés au moment du scan ; leur provenance dépôt → vault est établie par
le log du 17/08 (« Créées (dépôt jarvis-voice)… », puis « Vault : [[_jarvis-voice]] »).
Commitées sur ce constat, dans l'ordre qui les rend dérivées définitivement — **dépôt
d'abord, vault ensuite**, la règle que le détecteur affiche désormais lui-même.

## Commits

- Dépôt `jarvis-voice` : `d6a3288` (frontière datée) puis `38b1a61` (migration LLM du
  17/08), poussés sur `lixyra05-debug/jarvis-voice`.
- Vault : le commit qui porte ce log (pivot 17-18/08, logs des 17 et 18/08).

## Vérification d'acceptation

Elle exige les trois commits passés, donc elle court **après** le commit de ce log :
re-scan de `RESULTAT.md` + `MIGRATION-LLM.md`, attendu **0 fuite, 0 indéterminée, dérivés
seuls, exit 0**. Résultat rapporté à Hector en fin de session.

## Décisions en attente de GO

- Aucune nouvelle. Reste ouverte du 15/08 : la validation section par section de
  [[Douze pannes silencieuses]] — à laquelle le 17/08 a ajouté un candidat (le banc
  hérité, famille I).

---

# Session du 2026-08-18 (troisième session — test vocal, panne n° 13, lot 2 validé)

*Ajoutée en fin de fichier, sans rien retoucher au-dessus : R7.*

Mission : le test vocal d'Hector a montré le modèle bon (4/4 appels bien formés) et
l'outil cassé (`TOOL ERROR: SQLite objects created in a thread…`). Corriger proprement,
expliquer pourquoi le banc ne l'a pas vu, auditer les autres ressources, relancer — puis,
sur validation vocale d'Hector : commit + push, et les trois gestes vault demandés (GO
explicite, y compris pour le renommage de la page wiki).

## Lues

- Vault : [[_jarvis-voice]], [[2026-08-18-session]] (sections précédentes),
  [[La panne silencieuse]], la page wiki à renommer.
- Hors vault : `bench/vault_index.py`, `bench/vault_tool.py`, `s2s_pipeline.py`
  (chargement du module outil, thread principal), `audio_client.py` (exécution dans le
  thread du client, rattrape-tout ligne 528), `thread_manager.py`, le journal de la
  session vocale d'Hector.

## Modifiées / créées (dépôt jarvis-voice, hors vault)

- `bench/vault_index.py` — connexion SQLite **par thread** (`threading.local`), bâtie à
  la première recherche du thread depuis les chunks en mémoire (~42 ms, vault jamais
  relu). Écartés : `check_same_thread=False` (masque sans régler) et l'ouverture par
  recherche (base `:memory:` neuve = vide, erreur avalée en zéro résultat silencieux).
- `bench/smoke_vault_thread.py` — créé : reproduit la topologie de production (import
  dans le thread principal, `asyncio.run` dans un thread frais), reproduisait l'erreur
  mot pour mot avant correctif, vérifie l'identité des résultats entre threads.
- `bench/vault_tool.py` — le commentaire qui écartait le cas multi-thread comme
  hypothétique corrigé : c'était l'hypothèse fausse de la panne.
- `results/PANNES-SILENCIEUSES.md` — n° 13 ajoutée (famille I), titre passé à
  « Treize ». `RESULTAT.md` — troisième itération + section validation avec les mesures.
  `results/MIGRATION-LLM.md` — test vocal clos.
- Commits `984bb70` (correctif + n° 13) et `8e6d1c3` (validation + mesures), poussés.
  Scan d'avant-commit : 194 reprises, toutes dérivées, 0 fuite — la datation d'empreintes
  de la session précédente a fait son premier vrai service.

## Modifiées (vault)

- [[Treize pannes silencieuses]] — **renommée** depuis « Douze pannes silencieuses » sur
  GO d'Hector ; n° 13 ajoutée en famille I, bandeau et intro ajustés, le reste inchangé
  depuis le 15/08. La validation section par section reste ouverte.
- [[La panne silencieuse]] — le lien vers la page sœur suit le renommage (ligne annotée).
- [[_jarvis-voice]] — statut au 18/08 (lot 2 VALIDÉ), next action du test vocal cochée,
  décision datée du 18/08, entrée de journal. Les wikilinks des notes vivantes suivent le
  renommage ; **les trois logs qui citent l'ancien nom restent tels quels** (R7 — le lien
  y désigne la page telle qu'elle s'appelait ce jour-là).

## Mesuré

Le premier tour outillé réel de l'histoire du projet, au journal de la session vocale
d'Hector : fin de parole → transcription figée 338 ms → appel décidé +1 198 ms →
recherche +99 ms (première du thread, correctif à l'œuvre) → texte prêt +700 ms =
**2 335 ms** ; premier son ~2,5 s (estimé, le journal ne borne pas le PCM). 2 526
tokens/tour outillé → 3,2 tours/min à 8 000 TPM. Un échantillon, pas une médiane. Fait
neuf : ~260 tokens de raisonnement sur le tour qui parle, contre 6-15 sur celui qui
décide — la moitié que les bancs ne mesuraient pas.

## Décisions en attente de GO

- La validation section par section de [[Treize pannes silencieuses]] (ouverte depuis le
  15/08 ; la n° 13 est la seule section nouvelle depuis).

---

# Session du 2026-08-18 (quatrième session — lot 3, fin de la phase 2)

*Ajoutée en fin de fichier, sans rien retoucher au-dessus : R7.*

Mission : le lot 3 d'Hermes — UN seul outil de plus, en lecture seule sur la sonde
`hermes-collector`, avec les trois leçons du lot 2 appliquées d'emblée et le coût du
second schéma vérifié plutôt que déduit. Puis, sur validation vocale d'Hector : commit +
push, et la mise à jour du vault. Écriture dans `~/jarvis-voice` **et** dans le vault,
sur demande explicite.

## Lues

- Vault, lecture seule : [[_hermes-polymarket]] et sa fiche technique, [[_dream-console]]
  et sa fiche technique (contrat de la sonde, tunnel SSH), [[_jarvis-voice]], `AGENTS.md`.
- Hors vault : `~/dream-viewer/collector/hermes_collector.py` (endpoints, structure du
  snapshot, garanties), `audio_client.py` (contrat `load_realtime_tool_module`,
  rattrape-tout d'exécution), `bench/vault_index.py`, `bench/scan_avant_commit.sh`.

## Créées / modifiées (dépôt jarvis-voice, hors vault)

- `bench/hermes_tool.py` — l'outil `etat_systemes()`. `bench/outils.py` — l'agrégateur
  des deux outils (s2s ne charge qu'un module). `bench/smoke_outils_thread.py` — le
  smoke en topologie client. `bench/second_outil.py` — le banc de coût et de routage.
- `bench/vault_index.py` — correctif du classement. `bench/qualite_recherche.py` — les
  18 cas qui manquaient depuis le 13/08.
- `bench/scan_avant_commit.sh` — exception `127.0.0.1`, jugée sur le texte trouvé.
- `results/SECOND-OUTIL.md`, `results/CLASSEMENT-VAULT.md`, `RESULTAT.md`, `talk_fr.sh`.
- Commits `dd5e8d8`, `cfd0de8`, `54d40c3`, poussés sur `lixyra05-debug/jarvis-voice`.

## Modifiées (vault)

- [[_jarvis-voice]] — statut **phase 2 terminée**, quatre décisions datées du 18/08,
  next actions cochées (lot 3, test vocal), deux next actions ouvertes ajoutées
  (médiane du tour sans outil, définition de la phase 3), défauts audibles portés de
  deux à quatre, entrée de journal.

## Deux choses que le vault doit retenir au-delà de ce projet

- **Un chiffre mesuré sur un modèle ne se reconduit pas, il se remesure.** L'échafaudage
  d'outil de Groq valait ~208 tokens sur le 8B, il en vaut ~27 sur gpt-oss-20b. La
  conclusion du lot 2 tenait, sa prémisse chiffrée non — et seule une remesure pouvait
  le dire. Troisième occurrence du motif en deux jours (après le banc hérité et le
  détecteur de frontière).
- **Un contrôle qui échoue toujours ne se lit plus, puis laisse passer le jour où il a
  raison.** Vrai du détecteur de frontière avant le 18/08, vrai du motif « IP et
  hôtes » ce jour-ci. Le correctif n'est jamais « désactiver » : c'est réduire la portée
  à ce qui est justifiable, et **imprimer ce qu'on écarte** à côté du verdict.

## Un fait à consigner : le détecteur de frontière a attrapé une vraie fuite

Au scan d'avant-commit, deux fuites signalées — la phrase prononcée par l'assistant,
recopiée telle quelle dans `RESULTAT.md` et `results/SECOND-OUTIL.md` pour documenter le
test vocal. Elle portait les mots du vault (`01-Projects/vivo/fiche-technique.md`,
antérieure au dépôt de cinq semaines). **Reformulée, pas contournée** : les rapports
décrivent désormais ce qui a été cité sans le reproduire. Deuxième vraie fuite attrapée
par le détecteur daté depuis sa mise en service, et la première introduite par une
citation de bonne foi.

## Décisions en attente de GO

- La validation section par section de [[Treize pannes silencieuses]] (ouverte depuis le
  15/08). Aucune section nouvelle ce jour : les deux défauts audibles du 18/08 sont des
  **défauts recensés**, pas des pannes silencieuses — ils s'entendent.
- **La phase 3 reste à définir** : la phase 2 étant close, le prochain jalon est un
  arbitrage d'Hector, pas une suite évidente.

---

# Session du 2026-08-18 (cinquième session — phase 3 lot 1, le pont vocal)

*Ajoutée en fin de fichier, sans rien retoucher au-dessus : R7.*

Mission : relier `dream-viewer` à la voix — **l'affichage seulement**, pas la
navigation. Puis, sur validation vocale d'Hector : commit + push des deux dépôts, et
les entrées de vault. Écriture dans `~/jarvis-voice`, `~/dream-viewer` **et** dans le
vault, sur demande explicite.

## Lues

- Vault, lecture seule : [[_dream-console]] et sa fiche technique (contrat de la sonde
  Hermes, tunnel), [[_hermes-polymarket]], [[decisions-et-lecons]] de dream-console —
  c'est là qu'est la décision du 04/08 qui **suspend** la règle des effets sur l'accueil.
- Hors vault : `dream-viewer/AGENTS.md`, `lib/hermes.ts`, `components/hermes-context.tsx`,
  `components/home/{Home,Sphere}.tsx`, `app/globals.css` (les quatre keyframes
  autorisés), `scripts/home-shots.mts` ; côté serveur vocal `audio_client.py`,
  `serve_fr.py`.

## Créées / modifiées

- `jarvis-voice` : `bench/voix_sse.py` (le pont), `bench/smoke_voix_sse.py` (11/11),
  `bench/voix_scene.py` (rejoue un tour sans micro), `bench/serve_fr.py` (le
  branchement, coupable par `JARVIS_VOIX_PONT=0`). Commits `36b75b3`, `6046a79`.
- `dream-viewer` : `lib/voix.ts`, `components/voix-context.tsx`,
  `components/home/VoixBande.tsx`, la sphère modulée, la ligne du bas augmentée,
  `scripts/capture-voix.mts` (+ `npm run shots:voix`). Commit `84ff806`.
- Le détecteur de frontière, corrigé pour un second dépôt. Commit `2ab9315`.

## La décision de conception, et pourquoi elle n'est pas une contradiction

La décision du 04/08 **suspend** la règle « chaque effet encode une donnée nommable »
sur l'accueil, et seulement là. La consigne d'Hector la **rétablit** pour ce que la voix
y ajoute. Ce n'est pas une contradiction mais un **resserrage volontaire** : la
suspension est une permission, pas une interdiction d'être rigoureux. Les deux textes
coexistent sans se contredire, et les deux sont maintenant dans [[_dream-console]].

## Éprouvé avant de servir

| essai | attendu | obtenu |
|---|---|---|
| contrat HTTP du pont (GET, 405, 404, origine) | lecture seule vérifiable | ✓ 11/11 au smoke |
| machine à états sur un tour outillé | 9 transitions justes | ✓ |
| accueil **sans** pont (cas nominal) | rien ne s'affiche, rien ne casse | ✓ 60 fps, 0 erreur, mouvement réduit figé |
| accueil **avec** pont, quatre états | budget tenu | ✓ 60 fps partout |
| test à la voix | la boucle se ferme | ✓ **passé le 18/08** |

## Deux défauts trouvés dans le détecteur de frontière

En le pointant sur un second dépôt, deux faux verdicts sont apparus — tous deux du
genre « verdict faux plutôt qu'absent », le pire pour un contrôle :

- `DEPOT` était figé sur `jarvis-voice` : dater les fichiers d'un autre dépôt contre cet
  historique-là rendait « fuite » sur tout ce que l'autre porte légitimement.
- `EXT_TEXTE` ignorait `.ts`/`.tsx` : sur un dépôt TypeScript l'historique paraissait
  **vide**, donc toute phrase portée depuis des semaines était datée « non commité »,
  donc classée FUITE. **Seize faux positifs d'un coup**, quatorze redevenus dérivés
  après élargissement. Une liste d'extensions est un dénominateur comme un autre : ce
  qu'elle n'ouvre pas, elle le déclare absent — panne n° 9 sous une autre forme.

## Décisions en attente de GO

- ~~**Deux reprises dans `dream-viewer/components/home/Home.tsx`**~~ — **tranché par
  Hector le 18/08 : catégorie « doctrine », pas exemption au cas par cas.** Une règle de
  conception n'est pas de la matière de vault ; sa vocation est d'être citée dans le
  code qu'elle gouverne. `DOCTRINE` déclare la règle, son projet, sa date d'arbitrage et
  son motif ; la portée est bornée à la **suite de mots** — éprouvé, une fenêtre qui
  déborde d'un seul mot sur la prose voisine reste signalée — et ce qui est écarté
  s'imprime nommément. Commit `4ebc6cb`, poussé.
- **Phase 3 lot 2 — la navigation à la voix** n'est pas décidée. Elle romprait la
  lecture seule dans un sens : le cockpit cesserait d'être un afficheur.
- La validation section par section de [[Treize pannes silencieuses]] (ouverte 15/08).
